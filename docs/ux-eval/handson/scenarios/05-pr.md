# Phase 1 시나리오 — 05 Pull Request

> Plan #40 Phase 1 / 5번째 우선 — PR 영역은 git-fried 의 forge integration (Gitea + GitHub) 중심. GitKraken 의 PR UX = GitHub/GitLab/Bitbucket/Azure cross-forge. baseline 확인 = SB-037~042 backlog.
>
> backlog 매핑: SB-017 (PR CI 4 아이콘 인프라, Sprint c95+ Wave 1) + SB-037~042 (예상) + ARCH-001 PR CI wire (CRIT-001 다음 sprint)

## 영역 개요

GitKraken Sidebar 의 "PULL REQUESTS" 섹션 — Forge 연동 된 repo 의 PR list. Status (open / merged / closed) filter, CI status icon, reviewer list, comment count. PR row 클릭 → PrDetailModal (git-fried 대응) 또는 우측 panel 으로 detail.

## 시나리오 list (5건, outline)

### SC-05-1: PR list 표시 (open / merged / closed 분류)

- 사용자 액션: 좌측 sidebar 의 "PULL REQUESTS" 섹션 클릭 (collapsed → expanded)
- 예상 반응: PR list — 각 row = title + author avatar + status (open/merged/closed) + CI status icon (성공/실패/진행/대기) + comment count + age.
- git-fried 대응: useLaunchpad + PR list rendering. status filter UI 일치 검증.
- screenshot: 2개 (collapsed / expanded with multiple PRs)
- AHK: **S**
- 우선도 **HIGH** / backlog: SB-037 PR list parity

### SC-05-2: PR row hover — tooltip + CI status detail

- 사용자 액션: PR row hover
- 예상 반응: tooltip — full title + reviewer list + last activity. CI icon hover → 별도 popover (jobs list?).
- git-fried 대응: 현재 미구현 (후보). SB-017 PR CI 4 아이콘 인프라 (Sprint c95+) 의 다음 단계.
- screenshot: 3개 (hover row tooltip / hover CI icon popover / hover reviewer avatar)
- AHK: **M** (3개 hover area 별도 image anchor)
- 우선도 **MEDIUM** / backlog: SB-038 hover tooltip

### SC-05-3: PR 좌클릭 — Detail panel/modal 진입

- 사용자 액션: PR row 좌클릭
- 예상 반응: 우측 panel 또는 modal 으로 PR detail — 제목 + body + commits list + files changed + comments thread + approve/merge buttons.
- git-fried 대응: PrDetailModal.vue (466 LOC, Layer B template-heavy). 동일 흐름 검증.
- screenshot: 2개 (클릭 직후 detail 진입 / files changed 탭 클릭 후)
- AHK: **M**
- 우선도 **HIGH** / backlog: SB-039 PR detail parity

### SC-05-4: PR Approve / Request changes — 리뷰 액션

- 사용자 액션: PR detail 의 Approve 버튼 클릭 (또는 Request changes)
- 예상 반응: confirm dialog 또는 inline form (review message + approve/request). Submit → status update.
- git-fried 대응: useForgeMutation (GitHub/Gitea API). 동작 흐름 일치 검증.
- screenshot: 3개 (Approve 클릭 직전 / form / submit 후 status 변경)
- AHK: **L** (multi-step form 입력)
- 우선도 **MEDIUM** / backlog: SB-040 review action parity (의미 명확화 필요 — SB-040 raw 정의 확인)

### SC-05-5: PR Merge 버튼 — strategy 선택 (merge / squash / rebase)

- 사용자 액션: PR detail 의 Merge 버튼 클릭
- 예상 반응: dropdown 또는 dialog — Merge commit / Squash / Rebase 선택 + commit message edit + Merge 실행.
- git-fried 대응: 현재 forge merge mutation 미구현 (검증 필요). PR detail 의 Merge button 부재 가능성.
- screenshot: 3개 (Merge 클릭 직전 / strategy dropdown / merge 후 status)
- AHK: **L**
- 우선도 **HIGH** / backlog: SB-041 PR merge action 신규 후보

## Open question

- GitKraken 이 GitHub PR 의 review thread inline comment 까지 표시하는지
- CI status 가 multi-job 일 때 표시 방식 (icon aggregation vs expandable)
- "Convert to draft" / "Reopen" 같은 less-common action 의 menu 위치
