# 코드베이스 분석 리포트 — git-fried v0.3 release prep

- **생성일**: 2026-05-18 11:03:02
- **분석 모드**: `/analyze` (4 병렬 agent: 3 Claude Explore + 1 Codex Independent)
- **출력 길이 정책**: CLAUDE.md § Output Discipline — 500 lines+ 예상으로 파일 저장 + inline summary

## Rejected Claims (검증 패스)

- 재검증 결과 모든 subagent 주장 일치 — 기각 없음.
- spot-check 4건 모두 통과: `rsa 0.9.10` (Cargo.lock:4504), `tauri 2.10.3` ↔ `@tauri-apps/api 2.11.0` 미스매치, `merge.rs:40/51` from_utf8 우회, `ai/runner.rs:91` detect_clis 무 timeout.

## Codex Cross-Reference (4번째 병렬 agent — §2.6 cross-validation 결과)

> Agent 4 (Codex Independent Analysis) 정상 emit. codex-cli 0.130.0 사용 가능, agentId `ab4fd8fa6f0784806`.

### Agreed (4 agent 일치 — 최고 confidence)

- **i18n 1343 leaf** ko/en 대칭 (Agent 1 / Agent 3 / 사용자 MEMORY / `i18n-leaf-count.mjs` 4 출처 일치)
- **`<script setup>` 채택 96%** + Pinia Setup API 단일 store (Agent 3 + Agent 4 wiring 확인)
- **secret_mask 12 patterns 적용** (Agent 3 enumerate + Agent 4 keyed OAuth 한계 명시)
- **NFC normalize 한글 안전** (Agent 3 `git.ts` 8 함수 적용 + Agent 4 `decode_korean_safe` 활용)
- **vitest 90 spec + e2e 11 spec** (Agent 1 / Agent 3 일치)
- **@tanstack/vue-query 5.100 + vue-virtual 3.13 ↔ Vue 3.5 호환** (peer range 확인)
- **CodeMirror 12 패키지 모두 v6 major 정합** (Agent 2 + Agent 4 — `lang-vue@0.1.3` 은 pre-1.0 정상)

### Claude-only (Codex 미발견 — confidence 1 단계 down)

- **unplugin-auto-import 0.18.6 → 21.0.0 (20 major behind)** — Agent 2 정량화. Codex 는 일반 의존성 sync 만 언급. `likely` 로 유지하나 호환성/Vue3 영향 별도 검증 필요.
- **unplugin-vue-components 0.27.5 → 32.0.0 (4 major behind)** — Agent 2 만 명시.
- **eslint 9.39.4 → 10.4.0** + **tailwindcss 3.4.19 → 4.3.0** — Agent 2 정량화, Codex 비포함.
- **vite.config.ts `esbuild.pure` (warn/error 보존, log/debug/info drop)** — Agent 3 L2 확인, Codex 미언급.
- **invokeWithTimeout.ts retry/timeout policy (30s/5min/NON_RETRYABLE_PATTERNS)** — Agent 3 단독 deep dive.

### Codex-only (3 Claude agent 미발견 — 5-Check 통과 + spot-check 검증)

> 본 항목은 Codex 가 algorithm/race/boundary 영역에서 strong 한 결과. 모두 spot-check verification 통과 (`additional verification recommended` 라벨).

1. **🔴 Tauri JS/Rust 버전 미스매치** — `@tauri-apps/api 2.11.0` + CLI `2.11.1` vs Rust `tauri 2.10.3`, npm `plugin-dialog 2.7.1` vs Rust `tauri-plugin-dialog 2.7.0`. Tauri 공식 문서는 JS/Rust 동일 minor + 플러그인 exact-sync 권고.
2. **🔴 rsa 0.9.10 RUSTSEC-2023-0071 (Marvin attack on RSA PKCS#1 v1.5 decryption)** — Cargo.lock:4504 확인. patched version 없음. transitive 경로 미파악 (likely `russh` / SSH key handling 의존).
3. **🟠 Commit Graph max_lane overflow** — `git/graph.rs:192` `max_lane: max_lane_used + 1` + `useGraphWidth.ts:20-22` graphWidth max 400px + `useGraphCanvasRenderer.ts:116` 클립 없음 → `maxLane > 50` 시 캔버스 밖 draw 가능. UX 만 영향, 데이터 손상 아님.
4. **🟠 Hunk/Line staging 동시성 토큰 부재** — `useHunkStageActions.ts:78` `applyPatch(repoId, a.patch, ...)` + `stage.rs:71` `git apply --cached -` raw patch 제출. blob/index SHA 검증 없음. 외부 git 작업 동시 실행 시 race 후보 (git 측 reject 로 데이터는 보호되나 UX 실패).
5. **🔴 `ai::runner::detect_clis` timeout 부재** — `runner.rs:91` `cmd.output().await` (timeout 없음). 메인 `ai_run` path 는 `AI_RUN_TIMEOUT_SECS=60` 적용 (line 128/182), but `claude --version` / `codex --version` 행 시 detect 무한 stall 가능. SAF-401 회귀 위험.
6. **🟡 secret_mask 임의 opaque OAuth token 미커버** — `secret_mask.rs:41-45` `CLIENT_SECRET`/`ACCESS_TOKEN`/`REFRESH_TOKEN` keyed assignment 는 cover. JWT/PAT 패턴 외 opaque token (e.g., Gitea AccessToken 형식) 은 미패턴. 현재 OAuth code 가 v1.x skeleton 이라 미활성, 활성화 시 leak 가능.
7. **🟡 한글 안전 변환 우회 (`merge.rs:40/51`)** — `entry_path = std::str::from_utf8(&entry.path).unwrap_or("")` + `String::from_utf8_lossy(blob.content())`. CP949 fallback 우회. Merge conflict 표시 시 한글 파일명/내용 mangle 가능.
8. **🟡 lockfile 내 @codemirror/view duplicate** — `bun.lock:117` top-level `6.42.1` + `bun.lock:1017-1029` nested `6.41.1`. major 동일 but extension graph 동일성 이슈 후보 (`@codemirror/view` 다중 인스턴스 시 extension equality 실패 가능).

### Disagreements (Codex 가 Claude finding REJECTED)

- **`notify 6.1` "unused" 단정** (Agent 2 의 5-Check 결과 `forward-looking dependency` → CONFIRMED IN-USE, plan/04 reservation). Codex 도 별도 dead crate 식별 없음 → **Agreed: dead 아님**. 자동 삭제 권고 금지.

> Disagree 시 보존적 판단 원칙 — 본 케이스는 Claude/Codex 모두 "유지" 결론으로 무충돌.

## 프로젝트 개요

- **이름**: git-fried
- **버전**: 0.3.0 (workspace root + apps/desktop 동일)
- **프레임워크**: Tauri 2 + Rust + Vue 3 (desktop Git 클라이언트, GitKraken 대체)
- **스타일**: Tailwind 3.4.19 + SCSS 일부
- **API**: Tauri IPC (173 `#[tauri::command]`) + Forge REST (Rust 측 reqwest)
- **테스트**: vitest 90 spec / Playwright e2e 11 spec / Cargo 261 PASS (MEMORY 기준)
- **i18n**: vue-i18n 11.4.2, ko/en 1343 leaf 대칭

## 구조 (validator SoT — 본 프로젝트는 toolkit 아님, 본 항목 N/A)

> `scripts/validate-toolkit.js` 는 git-fried 본체에 부재. ~/.claude/ toolkit 전용. git-fried 카운트는 raw glob 으로 정확.

```
apps/desktop/
├── src/                          # Vue 3 frontend
│   ├── components/  94 .vue       (god comp >300 LOC: StatusPanel 648, CommitGraph 613,
│   │                              PrDetailModal 466, GitKrakenToolbar 414, FullscreenDiffView 413,
│   │                              CommitDetailSidebar 399, BranchPanel 370, CommitMessageInput 358,
│   │                              RepoSpecificForm 333, CloneRepoModal 327)
│   ├── pages/        4 .vue       index / launchpad / repositories / settings
│   ├── composables/  125 use*.ts  + 68 .test.ts (54% test coverage)
│   ├── stores/       1            repos.ts (Pinia Setup API, localStorage persist)
│   ├── locales/      ko.json + en.json (1343 leaf 각)
│   ├── api/          git.ts (NFC normalize 8 함수 적용)
│   └── i18n/ types/ utils/ styles/ ai/
└── src-tauri/src/                # Rust backend (88 .rs / ~19,794 LOC)
    ├── 11 top-level .rs           main / lib / ipc / git / forge / ai / auth / auth_oauth /
    │                              menu / launchpad / profiles / alias / error / panic_hook /
    │                              secret_mask
    └── submodules
        ├── git/    36 files       clone/commit/branch/diff/merge/rebase/stash/graph/stage
        ├── ipc/    29 files       173 #[tauri::command] (forge:18 / status:12 / stash:10)
        ├── forge/  4 files        Gitea / GitHub REST
        ├── ai/     3 files        Claude/Codex CLI subprocess
        ├── storage/ 2 + migrations  SQLite via sqlx 0.8
        ├── importer/ 2 files      GitKraken import
        └── pty/    1 file         xterm.js + portable-pty 0.8

docs/
├── plan/         38 numbered    00-overview ~ 37 (UltraPlan v0.4 LLM-test)
├── solutions/    2 dirs         (sub-dir 카테고리: vue / rust / tauri / 등)
├── analyze/      6 reports      최신 2026-05-16-110711
├── code-review/  2 + folder     최신 2026-05-16-133000
└── api / design-context / ux-eval / IMPLEMENTATION-STATUS.md / QUICK_START.md

e2e/             11 spec.ts     smoke / commit / status / stash / worktree / repositories /
                                shortcuts / actions / settings / gitkraken-parity /
                                tauri-webdriver-smoke
scripts/         8 utility       i18n-leaf-count.mjs / cargo-rustup.mjs / re-verify.mjs / 등
bench/           3 files         criterion 0.5 perf bench
```

## 핵심 패턴

### 상태관리

- **Pinia Setup API** (`stores/repos.ts` 단일 store). localStorage persistence + caller-decision API (Pattern 9, sprint c63 토큰).
- **Vue Query** auto-import (`vite.config.ts:29-33`). 57 composable 에서 185 호출. **stale-time 3-tier**: REALTIME 2s / NORMAL 30s / STATIC 60s (`queryClient.ts`).
- **createGlobalState (@vueuse/core)** ✕ — grep 0 occurrence. Pinia 일원화.

### API 호출 (Tauri IPC + Forge)

- **invokeWithTimeout.ts 217 LOC** — 모든 `invoke()` 통과. Default 30s, long-running (`bulk_*`/`clone_*`/`fetch_*`/`pull`/`push`/`ai_*`/`maintenance_*`/`import_gitkraken_apply`) 5min.
- **Retry policy**: exponential backoff 800ms × 1회. NON_RETRYABLE: 401/403/auth/timeout/validation/user-cancel.
- **Fire-and-forget**: `retry: 0, timeoutMs: 0` (전역 에러 swallow).
- **NFC normalize**: `api/git.ts` 의 8 export 함수 (commit message / branch / tag / remote) macOS NFD → NFC 변환.

### 에러 처리

- **Frontend**: `registerGlobalErrorHandler.ts` → `app.config.errorHandler` + `useToast().error()` i18n fallback + `invoke('report_frontend_error')` → Rust tracing sink (`target=frontend`) + secret_mask 자동 적용 + Rust 측 50건/초 rate limit.
- **Rust**: `thiserror` enum (12 variants: Git/GitCli/Db/Http/Json/Path/RepoNotFound/WorkspaceNotFound/Encoding/Validation/RateLimit{provider,retry_after}/AuthExpired{provider}/Internal) + `anyhow` fallback → `AppError::Internal` hybrid. IPC serialization 표준화 (`{ kind, message, ... }`).

### 인증 / 시크릿

- **OS Keychain** (`auth.rs`) — `keyring 3.4` (Win Cred Mgr / macOS Keychain / Linux SecretService). Service `git-fried` + key `forge_kind|base_url|username`. DB 는 `forge_accounts.keychain_ref` 만 (평문 token 저장 0). PR-C.2 fallback: Linux D-Bus 미가용 → `Ok(None)` (앱 unblock).
- **secret_mask.rs 12 패턴**: GitHub PAT (`ghp_/gho_/ghu_/ghs_/ghr_`), GitLab (`glpat-`), AWS (`AKIA/ASIA`), private keys (`-----BEGIN ... PRIVATE KEY-----`), Slack (`xox[barpsoe]-`), JWT (`eyJ...`), Stripe (`sk_/pk_/rk_` live+test), Anthropic/OpenAI (`sk-ant-`/`sk-`), Google API (`AIza`), DB URL (`postgres://`/`mysql://` auth-embedded), 한국 주민등록번호 (`\d{6}-?[1-4]\d{6}`), 일반 env-var (`API_KEY=`/`SECRET_TOKEN=`).

### 컴포넌트 합성

- `<script setup>` 96% (94/98). `defineProps<T>` TypeScript safe 63개 확인 (L2 partial).
- Composable 대규모 추출: 185 useXxx (22,296 LOC). god comp 회피 — sprint c75 god comp 0 마일스톤 달성 후 c80 잠복 god 5 일괄 (893→490 LOC -45%).

### i18n

- `useI18n()` + `$t()` 혼용 76 파일. 하드코딩 한글 UI text 0개 (스팟체크). 한글 comments 만 잔존 (Sprint c25-3 / Phase 11-6 / plan/30 메모).

### 빌드 / 개발

- `vite.config.ts esbuild.pure: ['console.log', 'console.debug', 'console.info']` (release only — warn/error 보존, registerGlobalErrorHandler 의 stack trace 살리기).
- Chunk 분리: vendor-vue / vendor-query / vendor-ui / vendor-codemirror / vendor-cm-langs (lang-* dynamic import).
- Tauri dev port 1420 (tauri.conf.json 동기화), HMR 1421.

## 의존성

### 핵심 런타임 (JS)

| 패키지 | 현재 | 최신 | 갭 | 위험 |
|--------|------|------|-----|------|
| vue | 3.5.34 | 3.5.34 | - | - |
| pinia | 2.3.1 | 3.0.4 | +1 major | ◯ (Composition API breaking) |
| vue-router | 4.6.4 | 5.0.7 | +1 major | ◯ (navigation guards) |
| @tanstack/vue-query | 5.100.10 | 5.100.10 | - | - |
| @tauri-apps/api | 2.11.0 | 2.11.0 | - | ⚠ Rust 측 2.10.3 미스매치 (Codex finding 1) |
| reka-ui | 2.9.7 | 2.9.7 | - | - |

### 핵심 런타임 (Rust)

`tauri 2.10.3` / `tokio 1.40` / `git2 0.19` / `sqlx 0.8` / `serde 1.0` / `reqwest 0.12 (rustls-tls)` / `chrono 0.4` / `encoding_rs 0.8` (한글 안전) / `unicode-normalization 0.1` / `parking_lot 0.12` / `keyring 3.4` / `portable-pty 0.8` / `tracing` + `tracing-subscriber` / `criterion 0.5` (dev).

### 업데이트 가능 (breaking risk ◯)

| 패키지 | 현재 | 최신 | 갭 | breaking |
|--------|------|------|-----|----------|
| pinia | 2.3.1 | 3.0.4 | +1 major | ◯ |
| vue-router | 4.6.4 | 5.0.7 | +1 major | ◯ |
| unplugin-auto-import | 0.18.6 | 21.0.0 | +20 major | ◯◯ |
| unplugin-vue-components | 0.27.5 | 32.0.0 | +4 major | ◯◯ |
| eslint | 9.39.4 | 10.4.0 | +1 major | ◯ |
| tailwindcss | 3.4.19 | 4.3.0 | +0.9 | ◯ |
| @xterm/xterm | 5.5.0 | 6.0.0 | +1 major | ◯ |

### 미사용 의존성 후보 (5-Check 통과만)

- **`notify 6.1`** — 5-Check 결과 `forward-looking` (Cargo.toml 주석 + plan/04 `.git refs watch` 로드맵 보존). `verdict: REJECTED` (dead 아님). Codex agreement.
- 그 외 5-Check 통과한 진정 dead dependency 없음.

## Recommendations

### HIGH — 즉시 조치 후보

1. **Tauri JS/Rust 버전 정합 (Codex finding 1)** — `@tauri-apps/api 2.11.0` + CLI `2.11.1` vs `tauri 2.10.3` minor 갭. v0.3 release 전 정합 필요. **증거**: [Cargo.lock:tauri 2.10.3](apps/desktop/src-tauri/Cargo.lock) + [package.json:37](apps/desktop/package.json#L37). **액션**: `cargo update -p tauri` 또는 npm side 다운그레이드 → release prep 검토. **confidence**: certain. **⚠ User Decision**: lockfile 변경 폭 큼.
2. **`ai::runner::detect_clis` timeout 추가 (Codex finding 5)** — 메인 `ai_run` 은 60s timeout 적용, but probe 인 `detect_clis` 는 `cmd.output().await` 무 timeout. `claude --version` / `codex --version` 행 시 detect 무한 stall. **증거**: [runner.rs:91](apps/desktop/src-tauri/src/ai/runner.rs#L91). **액션**: `tokio::time::timeout(Duration::from_secs(5), cmd.output()).await` wrap. **confidence**: certain.
3. **rsa 0.9.10 RUSTSEC-2023-0071 (Codex finding 2)** — Marvin attack on PKCS#1 v1.5. patched version 없음. transitive 경로 확인 필요. **증거**: [Cargo.lock:4504](apps/desktop/src-tauri/Cargo.lock#L4504). **액션**: `cargo tree -i rsa` 로 의존 경로 파악 → `cargo deny` / `cargo audit` CI 추가 + 영향 평가 후 ignore policy 결정. **confidence**: likely. **⚠ User Decision**: dependency 제거 가능성 vs ignore.

### MEDIUM — 다음 스프린트 후보

1. **Commit Graph max_lane overflow 처리 (Codex finding 3)** — graph width 400px max 클리핑/스크롤/lane 압축 indicator 부재. **증거**: [graph.rs:192](apps/desktop/src-tauri/src/git/graph.rs#L192) + [useGraphWidth.ts:20-22](apps/desktop/src/composables/useGraphWidth.ts#L20-L22) + [useGraphCanvasRenderer.ts:116](apps/desktop/src/composables/useGraphCanvasRenderer.ts#L116). **액션**: dynamic graph width 또는 horizontal scroll, `maxLane > N` 시 lane 압축 + tooltip 노출. **confidence**: likely.
2. **Hunk/Line staging concurrency token (Codex finding 4)** — `applyPatch` 호출 시 blob/index SHA 미검증. **증거**: [useHunkStageActions.ts:78](apps/desktop/src/composables/useHunkStageActions.ts#L78) + [stage.rs:71](apps/desktop/src-tauri/src/git/stage.rs#L71). **액션**: patch request 에 `expected_index_sha` / `expected_blob_sha` 포함 → Rust 측 pre-apply check + UI 재시도 confirmation. **confidence**: likely.
3. **`merge.rs:40/51` 한글 안전 decode 라우팅 (Codex finding 7)** — merge conflict 표시 path 가 CP949 fallback 우회. **증거**: [merge.rs:40](apps/desktop/src-tauri/src/git/merge.rs#L40) + [merge.rs:51](apps/desktop/src-tauri/src/git/merge.rs#L51). **액션**: `decode_korean_safe(...)` + `from_utf8_lossy` 대신 wrapped helper. **confidence**: certain.
4. **`@codemirror/view` lockfile duplicate (Codex finding 8)** — top-level 6.42.1 + nested 6.41.1. **증거**: [bun.lock:117 + bun.lock:1017-1029](bun.lock#L117). **액션**: `bun why @codemirror/view` 로 트랜시티브 경로 파악 → `overrides` 또는 `resolutions` 로 6.42.1 강제. **confidence**: likely. extension graph 동일성 이슈 가능.
5. **secret_mask 임의 opaque OAuth token 패턴 추가 (Codex finding 6)** — 현재 keyed assignment 만. **증거**: [secret_mask.rs:41-45](apps/desktop/src-tauri/src/secret_mask.rs#L41-L45). **액션**: OAuth code v1.x 활성화 전 Gitea AccessToken 형식 (40 hex 등) pattern 추가. **confidence**: likely.

### LOW — 여유 있을 때

1. **`unplugin-auto-import 0.18.6 → 21.0.0` (20 major behind)** — Agent 2 단독 finding. Vue3 정합 + auto-imports.d.ts 재생성 영향. **confidence**: likely.
2. **`unplugin-vue-components 0.27.5 → 32.0.0` (4 major behind)** — 동일 카테고리. **confidence**: likely.
3. **`lib.rs invoke_handler` 명령 그룹화** — 173 commands flat list (Codex finding A). **액션**: 도메인 별 macro 또는 builder. v0.3 후 리팩토링. **confidence**: likely.

### User Decision (자동 액션 금지)

- **`apps/*` workspace 확장 의도** — 현재 desktop 단일. 향후 mobile/web/shared 추가 시 frontend types 분리 필요. **권장**: v1.x 로드맵 확정 시 결정.
- **`pinia 2.3.1 → 3.0.4` major bump** — Composition API breaking change. **권장**: v0.3 release 후 v0.4 마일스톤에서 검토.
- **`vue-router 4.6.4 → 5.0.7` major bump** — navigation guards breaking. **권장**: pinia 와 함께 검토.

## Coverage Audit (positive claim verification — §2.7)

| # | Claim | Depth | Verdict |
|---|-------|-------|---------|
| 1 | "Pinia Setup API 100%" | L3 | CONFIRMED — defineStore signature + 단일 store, 회피 케이스 없음 |
| 2 | "Vue Query 57 composable / 185 use" | L2 | CONFIRMED — grep auto-import + queryClient.ts policy 확인 |
| 3 | "invoke timeout/retry policy 통일" | L3 | CONFIRMED — `invokeWithTimeout.ts` 217 LOC 전수 적용 |
| 4 | "thiserror + Serialize 표준화" | L3 | CONFIRMED — error.rs 12 variants enum + IPC kind() |
| 5 | "OS keychain + Linux fallback" | L3 | CONFIRMED — auth.rs PR-C.2 명시 |
| 6 | "secret_mask 12 패턴 + KRN" | L3 | CONFIRMED — regex 배열 enumerate, test coverage 별도 |
| 7 | "NFC normalize 8 함수 적용" | L2 | CONFIRMED — git.ts import + 호출 위치 spot-check |
| 8 | "`<script setup>` 94/98 (96%)" | L2 | CONFIRMED — grep -l count 신뢰 |
| 9 | "defineProps<T> 63/98" | L1 | DOWNGRADED — 키워드 매칭만, 나머지 35 의 props 미정의/Options API 여부 미검증 |
| 10 | "하드코딩 한글 0 UI text" | L1 | DOWNGRADED — 1 file 스팟체크, 전수 grep 결과 미확인 |
| 11 | "Playwright POM 적용" | L1 | DOWNGRADED — `helpers.ts` 내용 미확인 |
| 12 | "vitest 90 + e2e 11" | L2 | CONFIRMED — file glob + playwright.config 참조 |
| 13 | "esbuild pure console.log (warn/error 보존)" | L2 | CONFIRMED — vite.config.ts:79-83 line-by-line |
| 14 | "@codemirror/* v6 정합" | L2 | CONFIRMED — package.json + lock cross-check |
| 15 | "createGlobalState 0 use" | L2 | CONFIRMED — repo 전체 grep miss |

**총 15 단정 중 CONFIRMED 12 / DOWNGRADED 3 / 회색지대 0**.

## Decision Triage Summary

- **autonomous-safe (0)**: 본 /analyze 는 진단 only. 모든 권고는 사용자 승인 필요.
- **needs-user (3)**: HIGH-1 (Tauri sync — lockfile 폭 큼), HIGH-3 (rsa CI ignore policy), Pinia/vue-router major bump (User Decision 블록).
- **needs-claude-judgment (5)**: HIGH-2 (detect_clis timeout — 작고 명확), MED-1~5 (graph/staging/decode/lockfile/secret_mask — 모두 명확한 패치 패턴).
- **skipped (0)**.

## 부록 — Agent 별 raw finding 요약

### Agent 1 (Structure)
- 6 finding 모두 verdict=CONFIRMED + confidence=certain.
- 핵심: 88 Rust files (19,794 LOC) / 94 .vue / 125 composables / 1 Pinia store / 1343 i18n leaf / 12 e2e spec / 90+ unit test.

### Agent 2 (Dependency)
- 핵심 finding: notify 6.1 = forward-looking (Cargo.toml 주석 plan/04 reservation). dev tooling 다수 outdated (unplugin 시리즈 심각). bun.lock 정상 존재.
- 미사용 후보 5-Check 통과 없음.

### Agent 3 (Pattern)
- 16 finding, 12 certain + 1 L2 partial + 3 L1 (downgrade 권고).
- High confidence: invoke timeout/retry, error handling, keychain, secret_mask, NFC, esbuild config.
- L1 downgrade: defineProps coverage, 하드코딩 한글 grep, Playwright POM.

### Agent 4 (Codex Independent)
- 13 finding, 모두 verdict=CONFIRMED. 6 certain + 7 likely.
- Codex-only 핵심: Tauri sync, rsa RUSTSEC, graph overflow, hunk staging race, detect_clis timeout, opaque OAuth mask, merge.rs decode bypass, lockfile @codemirror/view duplicate.
- agentId `ab4fd8fa6f0784806` — SendMessage 로 follow-up 가능.

## Next Suggestion

- HIGH 권고 3건 → `/plan tauri-sync-and-detect-clis-timeout` 으로 구현 계획을 세울까요?
- User Decision 블록 3건 → `/research pinia-3-migration` 또는 `/research rsa-rustsec-impact` 로 근거를 더 파볼까요?
- 본 리포트가 `docs/analyze/2026-05-18-110302.md` 에 저장됨 — `/doc-sync` 로 README / CHANGELOG / MEMORY 갱신 검토 가능.
