# 코드베이스 분석 리포트 — 2026-05-26 10:45

> 4-agent 병렬 분석 (Structure / Dependency / Pattern Explore + Codex Independent).
> Codex CLI: codex-cli 0.130.0 ready. Agent 4 정상 합류.

## Rejected Claims (재검증 패스)

- **Agent 2 — `vue-i18n@11.4.2 misplaced in devDependencies`**: REJECTED 까진 아니지만 **DOWNGRADED**. 실제 `apps/desktop/package.json:77` 에 vue-i18n 이 devDependencies 에 있는 것은 사실이나, unplugin-auto-import 가 vite plugin 으로 build 시 inject 하는 패턴이라 dependencies 이동이 필수인지 추가 검증 필요. action 자동 권장 X.
- **Agent 2 — `notify@6.1 unused`**: 메모리 + Cargo.toml comment 자체가 plan/04 reservation 으로 명시. "unused" 표현은 사실이지만 의도된 reservation 이므로 destructive 권고 (제거) 금지.

## Codex Cross-Reference (4번째 병렬 agent — §2.6 cross-validation 결과)

> Agent 4 (Codex Independent Analysis) 정상 합류. 8 finding 모두 코드에서 literal verification 통과.

### Agreed (4 agent 일치)

(없음 — Codex 가 Claude 3 agent 와 완전 다른 영역을 cover. Structure / Dependency / Pattern 정량 측정은 Claude agent 가 맡고, Codex 는 보안/race/정합성 finding 에 집중)

### Claude-only (Codex 미발견)

- Structure / Dependency / Pattern 의 정량 메트릭 일체 (component 102 / composable 129 / rust 94 / IPC 186 / vitest 91 / cargo 49 / i18n 1951라인) — 모두 Claude 3 agent measurement. confidence: certain 유지 (literal grep result).

### Codex-only (3 Claude agent 미발견 — 5-Check 통과 후 surface)

- **CRIT-A**: `read_file` 절대경로 traversal 미차단 (CRITICAL) — `apps/desktop/src-tauri/src/git/read_file.rs:42` `..` 만 검사 후 `:66` `repo.join(path)` 호출. Rust `Path::join` 은 두 번째 인자가 absolute 시 첫 인자 무시. 테스트도 traversal 만 검증 (`:99-110`).
- **HIGH-A**: `pull`/`push` 가 `remote`/`branch` 를 reject_dash_prefix 없이 argv 에 push. `git/sync.rs:109-114, 164-168`. 가드 함수 `git/path.rs:75` 가 있으나 호출 안 함. CWE-88.
- **HIGH-B**: `rebase_run` 의 `e.sha`/`e.subject` 가 newline/control char 검증 없이 todo 파일에 기입. `git/rebase.rs:133-142`. `\nexec /bin/sh` 같은 subject 주입 시 sequence-editor 가 추가 todo command 로 해석.
- **HIGH-C**: `pty_open` 이 IPC 의 `shell` + `cwd` 를 allowlist 없이 spawn. `ipc/pty_commands.rs:18-32` + `pty/mod.rs:320-329`. arbitrary process launch.
- **HIGH-D**: `apply_profile_binding` TOCTOU — `profile_apply::apply` (multi-step Git config write) → `clear_managed` → DB `set_repo_profile` 사이 await gap 에서 동시 binding 시 Git config 와 DB binding 불일치. `ipc/profile_commands.rs:79-96`.
- **HIGH-E**: `profile_apply::apply` cancellation/interleaving 불안전. snapshot → set_local 루프 → verification → provenance 의 await 사이에서 task drop 시 rollback 미실행, partial state 남음. `git/profile_apply.rs:165-213`.
- **HIGH-F**: plan/43 SSH 키 binding 이 fetch/pull/push 에 적용 안 됨. `GitRunOpts.ssh_key_path` 필드는 존재하나 sync.rs 가 `GitRunOpts::default()` 만 사용 → 기능 갭. `git/sync.rs:39, 56, 115, 170`.
- **MED-A**: `useCommitGraphHeader` mousedown listener leak. `watch` 의 else branch 만 removeEventListener, `onUnmounted` 없음. headerMenuOpen=true 상태로 unmount 시 누수. `composables/useCommitGraphHeader.ts:57-65`.

### Disagreements (Codex 가 Claude REJECTED)

- (없음. Codex 자체적으로 5번 finding "SQLx injection" 을 REJECTED 처리)

## 프로젝트 개요

- **이름**: git-fried (`@git-fried/desktop` workspace)
- **버전**: 0.3.0 (MIT, private)
- **타입**: Tauri 2 desktop application — GitKraken 대체 (Gitea 1급 / 한글 안전 / 멀티 리포)
- **Frontend**: Vue 3.5.34 + Pinia 2.3 + @tanstack/vue-query 5.100 + vue-virtual + CodeMirror 6.42 + xterm 5.5
- **Backend**: Rust + Tauri 2.1 (Cargo.lock 2.10.3) + git2 0.19 + sqlx 0.8 + tokio 1.52
- **Build**: Vite 7.3 + Vitest 4.1 + TypeScript 6.0 + Bun 1.1 (lockfile)
- **CHANGELOG 최신**: Unreleased — Sprint c77 scroll system 자율 fix wave 마지막 기재. 메모리는 c102 까지 진행 (CHANGELOG 가 약 25 sprint stale).

## 구조

### Frontend (`apps/desktop/src/`)

| 카테고리 | 카운트 | 비고 |
|---|---|---|
| components/*.vue | 102 | 17,997 LOC 총합 |
| composables/*.ts (non-test) | 129 | 단일 책임 분해 패턴 |
| pages/*.vue | 4 | index / launchpad / repositories / settings |
| stores/*.ts | 1 (+test) | useReposStore (Pinia, client state only) |
| api/ | wrapper layer | invokeWithTimeout / errors / queryClient |
| vitest test files | 91 | 약 1,217 describe/it block |
| locales/{ko,en}.json | 각 1,951 LOC | 1,777 leaf key 대칭 |

### Backend (`apps/desktop/src-tauri/src/`)

| 카테고리 | 카운트 | 비고 |
|---|---|---|
| 총 .rs 파일 | 94 | |
| 최상위 module | 16 | ai / alias / auth / auth_oauth / error / forge / git / importer / ipc / panic_hook / pty / secret_mask / storage / launchpad / menu / profiles |
| git/*.rs | 40 | 도메인 분리 (bisect / branch / cherry_pick / commit / compare / config_local / conflict_prediction / diff / file_history / graph / hide / hooks / identity_stats / lfs / maintenance / merge / profile_apply / profile_match / range_diff / read_file / rebase / reflog / remote / repository / reset / restore / runner / sparse / stage / stash / status / submodule / sync / tag / worktree / ...) |
| ipc/*.rs | 31 | 도메인별 command 모듈 |
| IPC handler 등록 | 186 | `lib.rs` invoke_handler! 매크로 |
| cargo test 모듈 | 49 | inline `#[cfg(test)] mod tests` (tempfile fixture) |

## 핵심 패턴

### 상태 관리

- **Client state**: Pinia 1 store (`useReposStore` — 활성 repo / tab / workspace). createGlobalState 미사용.
- **Server state**: @tanstack/vue-query 5.100 전용. `useRepositoryQuery` 팩토리가 `queryKey: computed([name, repoId, ...extra])` 표준화. 7 wrapper composable 사용.
- **Bulk invalidate**: `REPO_PREFIXES_TO_INVALIDATE = ['status', 'log', 'graph', 'branches', 'conflict-prediction']` SOT. `useAutoFetch` + 14 mutation composable 가 invalidate 호출.

### API & 에러

- `invokeWithTimeout.ts` 가 모든 Tauri IPC 호출 wrapping. 30s default / 5min long-running prefix (`clone_*`, `fetch_*`, `pull`, `push`, `bulk_*`, `ai_*`, `maintenance_*`). 1-retry on transient (auth/timeout/validation/rate-limit 제외).
- `errors.ts` 4 formatter + `detectGitHint()` 10+ 패턴 (한글 hint). SecureMode `MAX_RAW_LEN=400` 으로 path/ref 노출 제한.

### Vue 컴포넌트

- 102/103 components `<script setup lang="ts">`. provide/inject 0건 (props/emit 일관).
- 198 composable (vs 102 component = 1.94:1). caller-decision API + group collapse pattern (Pattern 8~14 toolkit).

### Rust

- `tracing` 90 call (manual `error!`/`warn!`/`info!`). `#[instrument]` **매크로 0 건** (메모리 "IPC 6" stale — 5-Check 후 0 확인).
- `panic_hook.rs` install 됨 — release 모드는 `secret_mask::mask_secrets()` 거쳐 backtrace masking.
- `sqlx::query()` runtime variant 일색 (no compile-time `sqlx!` macro). 수동 `try_get()` row parsing — `storage/mod.rs:6` comment 에서 정책 명시.
- `tokio::spawn` 6 call (ai/runner + git/bulk + git/cherry_pick). `spawn_blocking` 12 파일 (git CLI / DB / file I/O).

### i18n

- vue-i18n 11.4. ko/en 1,777 leaf key 대칭 (lefthook pre-commit `i18n-symmetry` 가드).
- 한글 hardcode 0건 (component template/logic). aria-label/comment/test fixture 만 한글.

### Test

- Frontend: vitest 91 test, composable unit + 외부 service mock (`vi.mock('@tanstack/vue-query')` 등). component snapshot 없음.
- Backend: cargo 49 inline `mod tests`. `tempfile::NamedTempFile` + `Db::open()` per-test isolation. Tauri MockRuntime 은 Windows WebView2 제약으로 비활성 (lib.rs:291-298 comment).

## 의존성

### Frontend 핵심 (apps/desktop/package.json)

- **runtime**: vue 3.5 / pinia 2.3 / vue-router 4.6 / @tanstack/vue-query 5.100 / reka-ui 2.9 (BaseTooltip 1 컴포넌트만 사용)
- **editor**: @codemirror/* 6.x (8 lang plugin + merge + theme-one-dark)
- **terminal**: @xterm 5.5 + addon-fit + addon-web-links
- **font**: @fontsource-variable/roboto-flex + @fontsource/noto-sans-kr (self-host)
- **build**: vite 7.3 / vitest 4.1 / typescript 6.0 / vue-tsc 3.2 (매우 최신, npm registry 검증 통과)
- **i18n**: vue-i18n 11.4 (devDependencies 위치 — § Rejected Claims 참조)

### Backend 핵심 (apps/desktop/src-tauri/Cargo.toml)

- **frame**: tauri 2.1 (Cargo.lock 2.10.3 resolved) + 6 plugin (deep-link / dialog / fs / notification / shell + 자체 deep)
- **git**: git2 0.19 (vendored libgit2 + openssl)
- **db**: sqlx 0.8 (sqlite + tokio + macros + migrate + chrono)
- **async**: tokio 1.52
- **transient**: rsa 0.9.10 (reqwest chain) — **RUSTSEC-2024-0336** RSA-OAEP timing — context-dependent severity, git client 환경에서는 low. reqwest 0.12.29+ 업그레이드 시 자동 해소.
- **reservation**: notify 6.1 (plan/04 file-system-watch — 의도된 미사용)

### Lockfile

- bun.lock: @codemirror/view 6.41.1 + 6.42.1 dual resolution (Bun scoped install, runtime dedup 가능 — § Rejected 참조)

## Recommendations (개선 제안)

### HIGH — 즉시 조치 후보

1. **CRIT-A `read_file` 절대경로 traversal 가드 추가** — 증거: [git/read_file.rs:42](apps/desktop/src-tauri/src/git/read_file.rs#L42) `..` 만 검사, `:66` `repo.join(absolute_path)` 시 absolute 가 덮어씀. 액션: `Path::new(path).is_absolute()` 거부 + `canonicalize` 후 `starts_with(repo)` 체크. 테스트 `:98-110` 도 absolute case 추가. confidence: certain.
2. **HIGH-B `rebase_run` todo 인젝션 차단** — 증거: [git/rebase.rs:133-142](apps/desktop/src-tauri/src/git/rebase.rs#L133-L142) `e.sha`/`e.subject` 가 newline 검증 없이 todo 파일 기입. 액션: `subject.replace(['\n', '\r'], ' ')` + `sha` 가 hex 40자 검증 (또는 `is_ascii_hexdigit` 가드). confidence: certain.
3. **HIGH-A `pull`/`push` reject_dash_prefix 적용** — 증거: [git/sync.rs:109-114, 164-168](apps/desktop/src-tauri/src/git/sync.rs#L109-L114) remote/branch argv push 시 가드 미적용 (`git/path.rs:75` 의 함수 존재). 액션: `args.push(reject_dash_prefix(r, "remote")?)` + `--end-of-options` 사이 (commit `6f86a14` 학습 적용). confidence: certain.
4. **HIGH-C `pty_open` shell/cwd allowlist** — 증거: [ipc/pty_commands.rs:18-32](apps/desktop/src-tauri/src/ipc/pty_commands.rs#L18-L32) → [pty/mod.rs:320-329](apps/desktop/src-tauri/src/pty/mod.rs#L320-L329). 액션: `cwd` 가 등록된 repo root 의 하위경로인지 검증 + `shell` 을 시스템 shell allowlist (`cmd.exe`, `powershell.exe`, `bash`, `zsh`, `fish`) 로 제한. confidence: certain.
5. **HIGH-F plan/43 SSH 키 wire 누락** — 증거: [git/sync.rs:39, 56, 115, 170](apps/desktop/src-tauri/src/git/sync.rs#L115) 모두 `GitRunOpts::default()` 사용, `ssh_key_path` 미주입. 액션: `apply_profile_binding` / `apply_repo_override` 흐름과 동일하게 repo→profile→ssh_key resolve 후 `GitRunOpts { ssh_key_path: Some(...), ..Default::default() }` 전달. confidence: likely (Codex finding, runtime smoke 확인 권장).

### MEDIUM — 다음 스프린트 후보

1. **HIGH-D apply_profile_binding TOCTOU** — 증거: [ipc/profile_commands.rs:79-96](apps/desktop/src-tauri/src/ipc/profile_commands.rs#L79-L96). 액션: `state.db.with_repo_lock(repo_id, async { ... })` 같은 per-repo async Mutex 도입 (또는 SQLite advisory lock). confidence: likely.
2. **HIGH-E profile_apply::apply 취소 안전성** — 증거: [git/profile_apply.rs:165-213](apps/desktop/src-tauri/src/git/profile_apply.rs#L165-L213) snapshot/set/verify/provenance 사이 await drop 시 rollback 미실행. 액션: `tokio::select!` cancel path 에서 rollback 호출 + scope guard 패턴 (`scopeguard::defer`). confidence: likely.
3. **메모리 drift 수정 — `god comp 0`**: 실측 `components/*.vue` ≥200 LOC = **35개** (StatusPanel 668 / CommitGraph 622 / PrDetailModal 523 / GitKrakenToolbar 472 / FullscreenDiffView 433 / CommitDetailSidebar 399 / CommitMessageInput 395 / BranchPanel 384 / RepoSpecificForm 333 ...). 메모리 sprint c67/c75 "god comp 0 달성" 은 stale 또는 다른 threshold 기준. 액션: MEMORY.md sprint pointer 정정 + 향후 sprint 에서 ≥200 LOC threshold 재정의 (현 cap 250 LOC?). confidence: certain (literal grep).
4. **메모리 drift — `#[instrument] IPC 6`**: 실측 0건. 메모리 sprint c66/c78 표기 stale. tracing 은 manual `tracing::info!` 사용. 액션: MEMORY 정정 또는 `#[instrument(skip(state))]` 재도입 결정. confidence: certain.
5. **CHANGELOG 최신화** — 증거: CHANGELOG.md Unreleased 가 c77 까지만, MEMORY 는 c102 까지. 약 25 sprint backlog. 액션: `/doc-sync` 또는 다음 release tag 전 batch update. confidence: certain.

### LOW — 여유 있을 때

1. **MED-A useCommitGraphHeader listener cleanup** — 증거: [composables/useCommitGraphHeader.ts:57-65](apps/desktop/src/composables/useCommitGraphHeader.ts#L57-L65). 액션: `onScopeDispose(() => window.removeEventListener('mousedown', onHeaderMenuOutside))` 추가. confidence: likely.
2. **rsa@0.9.10 RUSTSEC-2024-0336** — context-dependent severity low (git client 환경). reqwest patch 추적. confidence: certain.
3. **bun.lock @codemirror/view 6.41.1+6.42.1 dual** — `bun install --verbose` 로 실제 bundle dedup 확인 후 결정. confidence: uncertain.

### User Decision (자동 액션 금지)

- **`vue-i18n` 의 devDependencies 위치** — runtime import (`useI18n`) 존재하나 unplugin-auto-import 가 vite plugin 으로 동작. 이동이 build 동작에 영향 줄 수 있음. 가능 액션: (a) dependencies 이동 (b) 현 상태 유지 (build 동작 확인된 경우) (c) 명시 import 만 사용으로 변환. 권장: 사용자 판단 + smoke test 후 결정.
- **`notify@6.1` 보유** — plan/04 reservation 명시. 가능 액션: (a) 유지 (b) plan/04 진행 시까지 commented-out (c) 제거. Cargo.toml comment 가 이미 명시이므로 **유지 권장** (자동 제거 금지).
- **god comp ≥200 LOC = 35개 정정 후 후속**: 단순히 메모리 정정만 할지 / threshold 를 어디로 잡을지 (≥200? ≥300? ≥500?) / 재추출 sprint 우선순위. 권장: 사용자 판단.

## Coverage Audit (positive claim verification)

| # | Claim | Depth | Verdict |
|---|---|---|---|
| 1 | "god comp 0 달성 (메모리)" | L3 | DOWNGRADED — substrate diff 후 35건 ≥200 LOC 확인. 메모리 stale. |
| 2 | "#[instrument] IPC 6 (메모리)" | L3 | DOWNGRADED — substrate diff 후 0건. 메모리 stale. |
| 3 | "vue-i18n 1777 keys 100% parity (Agent 3)" | L2 | CONFIRMED — wc -l ko=en=1951 (대칭). |
| 4 | "한글 hardcode 0건 (Agent 3)" | L2 | CONFIRMED — grep [가-힣]{5,} 결과 aria-label/comment/fixture 만 hit. |
| 5 | "createGlobalState 0건 (Agent 3)" | L2 | CONFIRMED — grep 결과 0. uncertainty 1단계 down (likely → certain). |
| 6 | "Pinia 1 store (Agent 3)" | L2 | CONFIRMED — stores/ 디렉토리 useReposStore.ts + test 만. |
| 7 | "vitest 91 / cargo 49 test files (Agent 1)" | L2 | CONFIRMED — find count 일치. |
| 8 | "IPC handler 186 (Agent 1)" | L2 | CONFIRMED — lib.rs invoke_handler 카운트 일치. |

총 8 positive 단정 중 CONFIRMED 6 / DOWNGRADED 2 (메모리 drift).

---

## Next Suggestion Rules

- HIGH Recommendations 5건 (모두 보안 관련) → **`/plan plan/43-followup-security-hardening` 으로 5건 구현 계획을 세울까요?**
- User Decision 3건 → 사용자 직접 결정 영역 (vue-i18n 이동 / notify 처리 / god comp threshold).
- CHANGELOG 25 sprint stale → **`/doc-sync` 로 CHANGELOG 일괄 정리 권장.**
- MEMORY god comp / #[instrument] drift → MEMORY.md sprint pointer 2건 정정 또는 `/doc-sync`.
