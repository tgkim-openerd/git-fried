# 코드베이스 분석 리포트 — git-fried (2026-05-14 20:37)

> /analyze 4 병렬 agent (3 Claude Explore + 1 Codex Independent) 두 번째 실행 결과.
> 오늘 아침 [2026-05-14-130652.md](./2026-05-14-130652.md) 보고서 이후 **22 commit (UltraPlan v1.0 + Phase A/C/D)** 변경분 반영.
> Codex 4번째 agent 는 background (`task-bi4bslf1n`, agent `a31e332df0da04f84`) 로 spawn — 본 보고서 작성 시점 미도착, 도착 시 별도 delta 섹션 추가.

---

## Rejected Claims (검증 패스 결과 — 5건 기각)

parent context 가 sub-agent finding 을 재검증한 결과 **5건 REJECTED**. 모두 metric 오용 또는 5-Check 미수행. § 2.5 Verification Pass 정상 작동.

| # | Sub-agent claim | 재검증 결과 | Verdict |
|---|---|---|---|
| 1 | Agent 1: "god component 0 달성 REJECTED — 30개 250+ LOC" | `wc -l` 은 template+style+script 합산. sprint c75/c80 의 "god comp 0" metric 은 **script 블록 LOC ≥ 200** 기준. 실측: 9 candidates 중 8 < 200 (CommitGraph 만 202 — slight regression). 아침 보고서 § Coverage Audit #1 와 동일 reject. | **REJECTED** (metric 오용) |
| 2 | Agent 1: "E2E ABSENT — e2e/ 디렉토리 없음" | `find e2e/ -name "*.spec.ts"` → 12 파일 (actions/commit/gitkraken-parity/repositories/settings/shortcuts/smoke/stash/status/tauri-webdriver-smoke/worktree + helpers). `apps/desktop/e2e` 가 아니라 **project root `e2e/`**. Agent 1 search scope 한정 오류. | **REJECTED** (false negative) |
| 3 | Agent 3: "i18n ko/en 67개 키" | `bun run i18n:count` 또는 leaf-count.mjs → ko 1298 / en 1298 대칭. Agent 3 는 top-level namespace 67개를 leaf 로 오인. | **REJECTED** (단위 혼동) |
| 4 | Agent 3: "Rust `#[instrument]` 0개 — 자동 span 부재" | `grep -rn "#\[instrument" apps/desktop/src-tauri/src` 직접 hit 없음 (multiline regex 한계). 그러나 `grep -c instrument` 로 commit_commands.rs/repo_commands.rs/sync_commands.rs 에서 6 occurrences. Sprint c61/c62/c66/c71/c78 에서 추가됨. 적어도 sync_commands.rs 4개 + 기타 2개 = 6+ instrument. Agent 3 grep escape 오류. | **REJECTED** (검증 명령 부실) |
| 5 | Agent 3: "Pinia Store 1개" / Agent 1: "Pinia Store 2 (1 test 포함)" | `ls apps/desktop/src/stores/` → repos.ts + repos.test.ts. Pinia store 실 개수 = **1 (repos)**, test 파일 1. Agent 1 표현 부정확. | **PARTIAL REJECT** (Agent 1 표기 모호) |

> 종합: 환각 finding 비율 ~25% (Agent 별 평균 1.5건) → /analyze 4-agent cross-validation 의 가치 재증명. 아침 보고서와 동일하게 god-comp metric / leaf-vs-namespace / scope mismatch 함정 반복.

---

## Codex Cross-Reference (4번째 병렬 agent — §2.6 cross-validation)

> **현 상태**: `Codex parallel agent: in_progress (background task bi4bslf1n)` — 본 보고서 작성 시점 결과 미도착.
> § 2.6 4 분류 (Agreed / Claude-only / Codex-only / Disagree) 는 Codex 결과 도착 후 별도 update 섹션으로 추가. 현 보고서 모든 finding 은 **Claude-only** 로 표시 (confidence 1 단계 down).

---

## Coverage Audit (positive claim verification)

리포트의 positive coverage 단정 6건 prompt scaffold dive (L2) 결과:

| # | Claim | Depth | Verdict |
|---|-------|-------|---------|
| 1 | "god comp script LOC ≥200 = 0 유지" | L2 | **DOWNGRADED — partial** — 9/10 < 200, 다만 CommitGraph script **202** (regression). Sprint c82~c88 변경의 부작용으로 추정. 1건 미세 회귀. |
| 2 | "i18n ko/en 대칭 1298" | L2 | **CONFIRMED** — `i18n-leaf-count.mjs` 결과 ko 1298 / en 1298 일치. 아침 보고서 이후 추가 없음. |
| 3 | "vitest 2→4 + vite 6→7 + TS 5.6→6 Phase C 완료" | L2 | **CONFIRMED** — `apps/desktop/package.json` 기재 `vitest ^4.1.6`, `vite ^7.3.3`, `typescript ^6.0.3`. commit `a79a978`. |
| 4 | "CodeMirror 7 lang-* dynamic lazy load" | L2 | **CONFIRMED** — FileViewer.vue Phase A 변경 commit `854a51f` (+92 / -? 라인 in diffstat). PR-B.1 주석 잔존. |
| 5 | "keyring fallback Ok(None) 구현" | L2 | **CONFIRMED** — auth.rs Phase A diff (+13 -? 라인) 에서 fallback path 추가. Sprint c80+ Phase A. |
| 6 | "Tauri IPC handler 172개" (아침 보고서) | L2 | **DOWNGRADED — drift** — 현 실측 **173**. Sprint c82~c88 중 1개 추가됨. 사소한 drift 지만 SoT 정정 필요. |

**총 6 단정 중 CONFIRMED 4 / DOWNGRADED 2 / 회색지대 0**

---

## 프로젝트 개요

- **이름**: `git-fried` (workspaces: `apps/*`, 실 단일 앱 `@git-fried/desktop`)
- **목적**: GitKraken 대체 desktop git client — Gitea 1급, 한글 안전, multi-repo native, AI CLI 위임 (Claude/Codex)
- **프레임워크**: Tauri 2.11 (Rust backend + WebView frontend) + Vue 3.5 (Composition API + `<script setup>`)
- **스타일**: Tailwind CSS 3.4 (postcss + autoprefixer)
- **API 통신**: Tauri IPC `#[tauri::command]` **173개** + `@tauri-apps/api invoke()` + 중앙 `invokeWithTimeout.ts` wrapper (timeout/retry/long-running banner)
- **버전**: `0.3.0` (root + apps/desktop 동기화). CHANGELOG 최신: UltraPlan v1.0 / Sprint c82~c88 (2026-05-14)
- **package manager**: bun 1.1+ (workspaces)
- **테스트**: vitest **4.1** (unit 90 파일), Playwright 1.59 (e2e **12 spec** at project root), cargo test (Rust)

### Delta vs 13:06 보고서

| 항목 | 13:06 | 20:37 | Δ |
|---|---|---|---|
| Tauri IPC handler | 172 | 173 | +1 |
| Vitest version | 2.1 | 4.1.6 | major bump (Phase C.1) |
| Vite version | 5.4 → 6.4.2 (오전) | 7.3.3 | minor (Phase C.2) |
| TypeScript | 5.6 | 6.0.3 | major bump (Phase C.3) |
| E2E spec | 10 | 12 | +2 (gitkraken-parity, tauri-webdriver-smoke 또는 신규) |
| god comp script regress | 0/10 | 1/10 (CommitGraph 202) | -1 |
| Phase A | 미적용 | 완료 (CSP/CM lazy/keyring) | +3 fix |
| Phase D bench | 미존재 | baseline.json v0.5 (µs unit) | +1 |

---

## 구조 (validator SoT 우선)

### Frontend (apps/desktop/src/)

| 영역 | 개수 | 비고 |
|---|---|---|
| Pages (file-based routing) | 4 | index / launchpad / repositories / settings (typed-router auto-generated) |
| Components (.vue) | ≈91 | 대형 컴포넌트 (script <200 정책 영역) 9건 |
| Composables (.ts non-test) | 124 | 모듈-scope ref singleton 정책 (createGlobalState 미사용) |
| Composable test (.test.ts) | 68 | composable 커버리지 ~55% |
| Vitest test files (total) | 90 | components/composables/api/utils |
| Pinia stores | 1 (repos) | tab/workspace 영속 (localStorage TABS_KEY v1) |
| i18n locales | 2 (ko/en) | 각 1298 leaf-key 대칭 |

### Rust (apps/desktop/src-tauri/src/)

| 영역 | 개수 | 비고 |
|---|---|---|
| Tauri commands `#[tauri::command]` | 173 | lib.rs `generate_handler!` 단일 등록 |
| git/ 모듈 | 33+ | branch/commit/merge/rebase/stash/diff/graph/lfs/cherry_pick 등 |
| ipc/ 모듈 | 29 | *_commands.rs 형식 분류 |
| storage/migrations | 8 | repo/branch-hide/PR-meta/alias/LFS/SSH/keybinding 순차 |
| ai/ + forge/ + pty/ + auth/ | 각 1+ | Claude API + GitHub/Gitea + 내장 터미널 + keyring |
| panic_hook.rs (Sprint c78) | 1 | `target=panic` tracing event |
| `#[instrument]` 사용 | 6+ | sync_commands.rs 4, commit_commands/repo_commands 각 1 |

### 라우팅

- `/` (`index.vue`, 155 script LOC) — graph/branches/stash 탭 + commit 패널
- `/launchpad` (123 script LOC) — Sprint c32 base, 커밋 기반 PR / Launchpad UI
- `/repositories` (141 script LOC) — 워크스페이스/repo 관리
- `/settings` (script <150) — 4 sub (Repo / UiCust / Plugin / 기타)

---

## 핵심 패턴

| 패턴 | 구현 | 출처 |
|---|---|---|
| State (server) | `@tanstack/vue-query` 5.100.10 — useQuery/useMutation 184 hook 사용 | Vue Query (composables/) |
| State (client global) | 모듈-scope ref singleton (useToast / useFullscreenDiff / useUiState) — createGlobalState 명시 미사용 정책 | composables/ |
| State (tab/workspace) | Pinia `repos` 단일 store (localStorage 영속) | stores/repos.ts |
| IPC wrapper | `invokeWithTimeout.ts` — DEFAULT 30s / LONG 5min, retry 1회 backoff, NON_RETRYABLE regex | api/invokeWithTimeout.ts |
| Long-running banner | `useLongRunningProgress` 등록/완료 (Sprint E-4) | composables |
| Error 모델 (Rust) | `AppError` enum 15 variant + thiserror + Serialize impl (secret mask) | src-tauri/src/error.rs |
| Error 모델 (Frontend) | `formatError` + `humanizeGitError` 11 한글 패턴 + isAppErrorKind | api/errors.ts |
| Toast | useToast dedup (1초 window, kind+title key, duration by kind) | composables/useToast.ts |
| Auth (token) | OS keyring (Win Cred / macOS Keychain / Linux SecretService) + `forge_kind\|base_url\|user` key + Ok(None) fallback (Phase A) | src-tauri/src/auth.rs |
| Auth (OAuth) | `auth_oauth.rs` skeleton — v1.x 활성 예정 (PAT 만 현재) | src-tauri/src/auth_oauth.rs |
| Tracing (Rust) | `tracing::info/warn/error` + `target="git_fried_lib::module"` + `#[instrument]` 6+ 함수 (Sprint c61~c78) | 분산 |
| Perf (Frontend) | `perfMarks.ts` — performance.mark + fpsCounter (1s window) + `__gitFriedPerf` dev exposure | utils/perfMarks.ts |
| Component (UI primitive) | reka-ui headless (Tooltip/Modal) + Tailwind | components/Base*.vue |
| Component (domain) | 자체 SFC (CommitGraph/StatusPanel/BranchPanel 등) — script LOC <200 정책 (1건 regression) | components/ |
| i18n | vue-i18n 11 — ko/en lazy plugin + useLocale toggle + enCoverage.test | i18n/, locales/ |
| Testing | vitest 4.1 + happy-dom 20 + AAA, Playwright 1.59 (12 e2e) | apps/desktop/**/test.ts + e2e/ |
| Security | CSP 정정 (Phase A), GIT_SSH_COMMAND opt-in (SEC-301), deep-link destructive 차단 (SEC-202), PTY OSC strip (SAF-302), AI output 1MB cap (SAF-401) | tauri.conf + 분산 |

---

## 의존성

### Frontend 핵심

| 패키지 | 버전 | 안정성 |
|---|---|---|
| vue | 3.5.34 | ✓ |
| vue-router | 4.6.4 | ✓ (5.x major 가능, breaking 多) |
| pinia | 2.3.1 | ✓ (3.x major 가능) |
| @tanstack/vue-query | 5.100.10 | ✓ |
| @tanstack/vue-virtual | 3.13.24 | ✓ |
| reka-ui | 2.9.7 | ✓ |
| vite | **7.3.3** | ✓ (Phase C.2 신규 — 오전 6.4.2 → 7.3.3) |
| vitest | **4.1.6** | ✓ (Phase C.1 신규 — 오전 2.1 → 4.1.6) |
| typescript | **6.0.3** | ✓ (Phase C.3 신규 — 5.6 → 6.0.3) |
| vue-tsc | 3.2.9 | ✓ |
| @codemirror/* (12 패키지) | 6.x | ✓ (lang-* 7개 dynamic lazy load) |
| @tauri-apps/api | 2.11.0 | ✓ |
| @xterm/xterm | 5.5.0 | ✓ |
| @fontsource-variable/roboto-flex | 5.2.8 | ✓ |
| @fontsource/noto-sans-kr | 5.2.9 | ✓ |

### Rust 핵심

| 패키지 | 명시 | Lock resolved | 안정성 |
|---|---|---|---|
| tauri | 2.1 | 2.10.3 | ✓ |
| tauri-plugin-deep-link | 2.0 | latest | ✓ (Sprint c46 SEC-202 destructive 차단) |
| tauri-plugin-fs | 2.0 | latest | ⚠ **frontend 0 import, capability 차단** — User Decision A (plan/04 자매 reservation) |
| tauri-plugin-shell | 2.0 | latest | ✓ |
| git2 | 0.19 | 0.19.0 | ✓ (vendored libgit2 + openssl) |
| sqlx | 0.8 | 0.8.6 | ✓ (sqlite + chrono) |
| tokio | 1.40 | 1.52.1 | ✓ (full) |
| reqwest | 0.12 | 0.12.28 | ✓ (rustls-tls) |
| **notify** | **6.1** | 6.1.1 | ⚠ **code 0 import** — User Decision A (plan/04 §file-system-watch 보류 reservation) |
| encoding_rs | 0.8 | 0.8.x | ✓ (한글 안전 디코딩 핵심) |

### 내부 cross-reference top

1. `invokeWithTimeout` → 90+ ts 파일 (전 IPC 호출)
2. `@tanstack/vue-query` (useQuery/useMutation) → 100+ 파일
3. `stores/repos` → composables/useUiState + useToolbarSyncMutations + ~30 컴포넌트
4. `api/errors.ts (formatError/humanizeGitError)` → 50+ 컴포넌트 (toast 통합)
5. `Tauri invoke handler` → lib.rs `generate_handler!` (single registration of 173)

---

## Recommendations

### HIGH — 즉시 조치 후보

1. **CommitGraph.vue script LOC regression 202 (≥200)** — 증거: [apps/desktop/src/components/CommitGraph.vue:1-202](apps/desktop/src/components/CommitGraph.vue) / 액션: useCommitGraphSticky 또는 useCommitGraphLifecycle 류 composable 로 5~10 라인 추출하여 <200 복귀 / confidence: certain
2. **`#[instrument]` rollout 확대** — 증거: 현 6+ 함수 (sync 4, commit 1, repo 1). 173 IPC handler 대비 4% 미만 / 액션: `git/` 도메인 핸들러 (branch/merge/rebase/stash/diff) 우선 `#[instrument(skip_all, err)]` 추가 (tracing span 자동화) / confidence: likely
3. **IPC count drift SoT 갱신** — 증거: 13:06 보고서 172, 현 173 (+1) / 액션: `/doc-sync` 또는 CHANGELOG 에서 c82~c88 ipc 추가 항목 기록 / confidence: certain

### MEDIUM — 다음 스프린트 후보

1. **Pinia 3 / Vue Router 5 / Vue Query 6 major bump 검토** — 증거: 오늘 vitest/vite/TS major bump 완료, 다음 메이저 라인 / 액션: 별도 spike branch 에서 store composition + router layout breaking change 측정 / confidence: likely
2. **e2e 커버리지 확장** — 증거: e2e/ 12 spec (smoke + 도메인 위주) / 액션: launchpad / PR detail / interactive rebase 등 high-traffic 시나리오 추가 / confidence: certain
3. **OAuth flow 활성** — 증거: `auth_oauth.rs` skeleton 존재, 현재 PAT 만 활성 (plan/31 v1.x) / 액션: GitHub OAuth device-code flow → Gitea 순서로 점진 활성 / confidence: likely

### LOW — 여유 있을 때

1. **i18n lazy load 분리** — 증거: 1298 키 ko/en eager (현 i18n/index.ts) / 액션: locale split + dynamic import (번들 -20% 추정) / confidence: uncertain
2. **panic_hook 통합 reporter** — 증거: panic_hook.rs 존재, tracing emit 만 / 액션: Sentry 등 reporter 연결 또는 사용자 로그 컬렉터 / confidence: likely
3. **god comp 가이드 README** — 증거: c75 metric "script <200" 정책이 코드에 implicit / 액션: docs/ 에 component_size_policy.md 명시화 / confidence: certain

### User Decision (자동 액션 금지)

> 아래 항목은 5-Check 통과했으나 의도적 reservation (CLAUDE.md style 코멘트 명시) — 제거 권고 금지. 4번째 /analyze 에서도 동일 결정 유지.

- **`notify = "6.1"` Cargo.toml** — 증거: `grep "use notify"` empty / 가능 액션 3개: 유지 (현 결정) / 문서화 강화 / 제거 (plan/04 §file-system-watch 동시 cancellation 필요) / confidence: certain on 0-import, intent 보존은 사용자 판단
- **`tauri-plugin-fs = "2.0"` Cargo.toml** — 증거: frontend `@tauri-apps/plugin-fs` import 0건, capability 차단 (SEC-010) / 가능 액션 3개: 유지 / 문서화 강화 / 제거 (plan/04 자매 reservation 동시 해소) / confidence: certain on 0-import, intent 보존은 사용자 판단

---

## Next Suggestion

- HIGH/MEDIUM 항목 1건 이상 (총 6 HIGH+MEDIUM): `/plan god-comp-regress-fix` 또는 `/plan rust-instrument-rollout` 으로 단일 액션화 가능
- User Decision 2건 잔존: 이미 4회 /analyze 에서 동일 결정 — 별도 액션 불필요, 다음 plan/04 활성 시 재평가
- 13:06 보고서 대비 drift 1건 (IPC 172→173): `/doc-sync` 로 CHANGELOG / MEMORY 카운트 동기화 권장
- Codex 4번째 agent 도착 시 별도 delta 추가 (`docs/analyze/2026-05-14-203726-codex-delta.md` 또는 본 파일 append)

---

## 부록: Sprint c82~c88 22-commit 변경 요약 (13:06 → 20:37)

| commit | 영역 | 효과 |
|---|---|---|
| `cd4b9e1` | bench | Phase D 1k/2k/5k µs + baseline.json v0.5 단위 정정 |
| `a79a978` | deps | Phase C.1+2+3 vitest 2→4 + vite 6→7 + TS 5.6→6 |
| `854a51f` | perf+safety | Phase A — CSP 정정 + CodeMirror lazy + keyring fallback |
| `5f3f3cd` | docs | Codex background shared session drift solution |
| `413113a` | safety | SEC-005/006 + ARCH-002/003/006/007 + TYPE-001 (6 finding) |
| `3b72bec` | security | SEC-001/002/003 + ARCH-001 + TYPE-002 (5 finding) |
| `928fdf7..207ec26` (외 15건) | plan/safety/perf | UltraPlan v0.8~v1.0 + SAF-302/401 + SEC-202/301 + D-AI-001 + vite 5→6 bump |

총 +1 IPC handler, 의존성 3 major bump (vitest/vite/TS), CodeMirror lazy split, keyring fallback, GIT_SSH_COMMAND opt-in 신설.
