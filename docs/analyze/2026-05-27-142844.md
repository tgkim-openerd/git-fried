# 코드베이스 분석 리포트 — git-fried v0.3.0

> **실행**: `/analyze` (4 병렬 agent — 3 Claude Explore + 1 Codex Independent)
> **시점**: 2026-05-27 14:28
> **직전 sprint**: 2026-05-26~27 보안 강화 6 wave (`24fa100..1332a6e`) + UI 흡수 cycle C1~C3
> **재진입 트리거**: NEXT-SESSION.md Phase 0

---

## Rejected Claims (검증 패스 — § 2.5)

| # | Sub-agent claim | 재검증 결과 | 처분 |
|---|----------------|------------|------|
| 1 | (Claude Pattern agent) "`reactive` 신규 금지 — 확인 안 함 (CLAUDE.md 파일 없음)" | CLAUDE.md 는 user 전역 `~/.claude/CLAUDE.md` 에 존재 (current additionalContext 로 주입) | **REJECTED** — repo 내 CLAUDE.md 부재만 사실, 룰은 적용됨 |
| 2 | (Claude Structure agent) "`#[tracing::instrument]` 0개" | `grep -rn "#\[tracing::instrument" apps/desktop/src-tauri/src/ \| wc -l` = **6** | **REJECTED** — Sprint 2026-05-26 메모리 drift 정정 시점부터 6개로 증가 (commit_commands.rs:36 / repo_commands.rs:118 / sync_commands.rs:22,50,94,128) |
| 3 | (Codex agent) "god comp ≥200 LOC = 34건" | components/ 만 = 19, components/ + pages/ = 22 | **부분 REJECTED** — Codex 측 카운트 기준 미상, 실측 우선 |

> Claude Dependency agent / Codex agent 의 sqlx-only 단정은 storage/db.rs:6-7 의 `use sqlx::sqlite::*` 로 CONFIRMED 유지.

---

## Codex Cross-Reference (§ 2.6 cross-validation 결과)

> Codex (GPT-5.x) Independent Analysis 합류 — opnd-codex:codex-rescue 정상 emit.

### Agreed (4 agent 일치) — 최고 confidence
- **Tauri 2.1 사용 중** (1.x 아님) — 보안 패치 우려 없음
- **storage layer sqlx-only** (rusqlite 혼용 없음)
- **panic = "abort" 유지** — Windows +68% 회피 정착
- **secret_mask.rs 11 regex 패턴 + 16 unit test** — 보안 마스킹 정착
- **vue-query 5.100.10** — TanStack 3-tier STALE_TIME (2s/30s/60s)
- **NFC 정규화 `/api/git.ts` 전역** — 한글 안전성 정착

### Claude-only (Codex 미발견) — confidence 1 단계 down
- composables 129개 enumerate (Claude Structure) — likely → likely (Codex 도 numeric 추정만)
- vue-i18n 11.4.2 + reka-ui 2.9.7 patch 후보 (Claude Dependency) — likely → uncertain
- Pattern 8~14 framework 정착 사례 (Claude Pattern) — uncertain (문서명 매핑 불가, 보고서에서 제외)

### Codex-only (3 Claude agent 미발견) — 5-Check 통과 후 surface
- **HIGH-A `runner.rs:145-164` timeout orphan** — `wait_with_output()` 소유권 이전 후 timeout 시 child kill 불가 (CWE-404). 6 wave 미해소. **certain** (코드 주석 자체에 "best-effort kill 불가, orphan 가능" 명시)
- **HIGH-B forge `base_url` scheme 미검증** — 사용자 임의 `http://attacker.internal` 등록 가능 (keychain 저장 전). **likely**
- **MED-A forge owner/repo URL 세그먼트 인젝션** — `format!("/repos/{owner}/{repo}/pulls")` 직접 보간, `?#%2f` 미보호. **likely**
- **MED-B PTY cols/rows 경계 검증 없음** — `PtyOpenArgs.cols: u16` 0x0 또는 비정상 크기 그대로 전달. **certain**
- **MED-C importer repo path canonicalize 누락** — `ensure_trusted_profile_dir` 후 `PathBuf::from(stripped).exists()` 만, symlink traversal 가능. **likely**
- **LOW-A panic_hook 컨텍스트 부족** — file/line/payload 만 로깅, repo_id/session_id/operation 누락. spawn 사이트 (`git/bulk.rs:65`, `git/profile_apply.rs:178`, `ipc/pty_commands.rs:145`) wrap 권장. **likely**
- **USER-DECISION `wsl.exe` PTY allowlist** — 명령 분리자 필터 통과해도 WSL 내부 임의 Linux 명령 실행 가능. 의도적 설계? 명시 확인 필요. **likely**

### Disagreements (Codex 가 Claude finding REJECTED)
- (없음 — Codex 가 정정한 `tracing::instrument` 0개와 rusqlite 혼용은 § Rejected Claims 로 이미 처리)

---

## Coverage Audit (positive claim verification — § 2.7)

| # | Claim | Depth | Verdict |
|---|-------|-------|---------|
| 1 | "Forge enum 3-step seal (Sprint c95) 정착" | **L2** | CONFIRMED — Pattern agent 가 ForgeClient trait + ResponseForgeExt + AppError serialization 3 단계 모두 인용 |
| 2 | "Pinia + Vue Query 혼합 정착" | **L2** | CONFIRMED — `useReposStore` + STALE_TIME 3-tier + `gcTime: 5min` 실측 |
| 3 | "Pattern 8~14 (vue3-composable-extraction) 정착" | **L1** | DOWNGRADED — 문서명만 언급, 실 코드 매핑 미인용 |
| 4 | "secret_mask 11 regex + 한국 SSN 포함" | **L3** | CONFIRMED — 16 unit test (dashed variant + overmatch 방어) 실측 |
| 5 | "reject_dash_prefix CWE-88 가드 정착" | **L3** | CONFIRMED — bisect/branch/cherry_pick/commit/merge/remote/rebase/tag/stash 9 파일 enumerate |

총 5 단정 / CONFIRMED 4 / DOWNGRADED 1 / 회색지대 0

---

## 프로젝트 개요

- **이름**: git-fried (GitKraken 대체 데스크탑 git 클라이언트)
- **버전**: v0.3.0 (CHANGELOG 최신 항목)
- **프레임워크**: Tauri 2.1 + Vue 3.5.34 + Rust (workspaces `apps/desktop`)
- **스타일**: Tailwind 3.4.19 (v4 마이그레이션 미진행)
- **API**: 자체 IPC (`#[tauri::command]` 187개) + Gitea/GitHub Forge HTTP 클라이언트
- **OS 타겟**: Windows 우선 (panic="abort", PowerShell 환경)

---

## 구조 (실측 SoT)

| 영역 | 카운트 | 검증 |
|------|--------|------|
| **Vue 컴포넌트** | 102 (`apps/desktop/src/components/*.vue`) | `find ... -name "*.vue" \| wc -l` |
| **Composable** | 129 (test 제외) | `find composables -name "*.ts" -not -name "*.test.ts"` |
| **Vue pages** | 4 (`index/repositories/settings/launchpad`) | enumerate |
| **Rust `.rs`** | 94 (8 디렉토리 모듈 + 11 단일 파일) | `find src-tauri/src` |
| **IPC `#[tauri::command]`** | **187** (Sprint c100 기준 178 → +9 증가) | `grep -r "#\[tauri::command\]"` |
| **IPC 파일** | 31 (`ipc/*.rs`) | `ls` |
| **Git 연산 모듈** | 40 (`git/*.rs`) | `ls` |
| **`#[tracing::instrument]`** | **6** (Claude 보고 0 → REJECTED 후 정정) | commit_commands:36 / repo_commands:118 / sync_commands:22,50,94,128 |
| **Rust test 모듈** | 51 (`#[cfg(test)]`) | grep |
| **Vue vitest** | 91 | `find -name "*.test.ts" -o "*.spec.ts"` |
| **e2e (Playwright)** | 11 spec | `find e2e` |
| **docs/plan/** | 43 | `ls` |
| **docs/solutions/** | 5 (루트 직접) | `ls` (메모리 39와 drift — 메모리는 toolkit/global 포함 카운트 가능성, 재확인 권장) |

### God Component (≥200 LOC) — 실측 enumerate

| 위치 | LOC | 파일 |
|------|-----|------|
| **components/** Top 5 (refactor 1순위) | 670 / 637 / 523 / 472 / 433 | StatusPanel / CommitGraph / PrDetailModal / GitKrakenToolbar / FullscreenDiffView |
| **components/** Mid (Top 6-10) | 399 / 395 / 384 / 333 / 327 | CommitDetailSidebar / CommitMessageInput / BranchPanel / RepoSpecificForm / CloneRepoModal |
| **pages/** 3건 (Sprint c75 god comp 0 마일스톤과 drift) | 554 / 528 / 420 | launchpad.vue / repositories.vue / settings.vue |
| **components/ 총** | **19개 ≥200 LOC** | — |
| **src 전체 총** | **22개 ≥200 LOC** | (pages/ 3 포함) |

> ⚠️ **메모리 drift**: `god comp 0 전체 달성` (Sprint c75 마일스톤) 이후 추가/회귀로 22건 발생. CLAUDE.md `god comp 0` 룰 SoT 와 실측 drift. 다음 sprint 에서 메모리 [sprint_2026_05_12_c75] 업데이트 또는 god comp threshold 200 → 300/400 재정의 필요.

---

## 핵심 패턴

### 상태관리 (L2 정착)
- **Pinia** — `useReposStore()` 활성 repo tab + workspace (localStorage persist)
- **Vue Query (TanStack)** — 모든 IPC 응답 `useQuery` / `useMutation` 캐시. STALE_TIME 3-tier (REALTIME 2s / NORMAL 30s / STATIC 60s), `gcTime: 5min`, `refetchOnWindowFocus: true`
- **ref 우선** — `reactive` 신규 사용 없음 (CLAUDE.md 룰 준수)

### API 호출 (L3)
```
@tauri-apps/api/core invoke
  → invokeWithTimeout (DEFAULT 30s / LONG 5min for bulk/clone/fetch/pull/push/ai/maintenance)
  → Exponential backoff retry (clone/fetch/pull/push 1회, 인증실패 제외)
  → /api/git.ts (NFC 정규화 — Sprint c30, 한글 안전)
```
- 단건 API 호출 try/catch 금지 (composable 내부) — 검증 OK
- `useCommitMutation` 패턴 정착: useMutation + invalidate(['status'], ['graph'], ['branches']) + toast

### 에러 처리 (L2/L3)
- **Vue 측**: `/api/errors.ts` (400 LOC) — `describeError()` 한국어 hint 25+ 패턴 + 토큰 노출 마스킹 (SEC-001)
- **Rust 측**: `AppError` enum — AuthExpired / RateLimit (Retry-After 파싱) / GitCli / Validation. ResponseForgeExt 로 401/403/429 분기

### Forge / Profile (L2)
- ForgeKind: Gitea | GitHub — 3-step seal (ForgeClient trait + ResponseForgeExt + AppError serialization)
- per-repo override (plan/43): repo override → profile default → forge kind fallback chain
- `previewProfileApply()` dry-run + signing warning

### 보안 가드 (L3 — Sprint 2026-05-26~27 6 wave 직후)
- `secret_mask.rs` — 11 regex (GitHub PAT/GitLab/AWS/Slack/JWT/Stripe/Anthropic/OpenAI/Google/Database URL/한국 SSN) + env-var-secret generic + 16 unit test
- `reject_dash_prefix()` — bisect/branch/cherry_pick/commit/merge/remote/rebase/tag/stash 9 파일 적용 (CWE-88)
- `--end-of-options` — git 2.36+ 모든 ref/path 인자 (cherry_pick/reset/switch/merge/branch create-delete-rename-rebase)
- 직전 sprint 신규: TOCTOU per-repo lock + cancel-safe spawn + rev validation + read_file 절대경로 차단

### 컴포넌트 합성 (L2)
- `use*Actions` (BranchActions/CommitActions) — 비즈니스 로직 + IPC, ContextMenuItem[] 반환, callback 주입
- `use*Mutations` — useMutation + invalidate + toast + lastResult ref
- `use*` (Status/Branches) — useQuery + computed refKey

---

## 의존성 (실측 검증)

### Vue (apps/desktop/package.json)
| 패키지 | 현재 | 최신 | 비고 |
|--------|------|------|------|
| vue | 3.5.34 | ✓ | |
| vue-i18n | 11.4.2 | ✓ | 1597 leaf, ko/en 대칭 |
| @tanstack/vue-query | 5.100.10 | ▲ 5.100.14 patch | 80+ 참조 |
| @codemirror/view | 6.42.1 | ▲ 6.43.0 patch | |
| @xterm/xterm | 5.5.0 | ⚠️ 6.0.0 breaking | 터미널 |
| pinia | 2.3.1 | ⚠️ 3.0.4 major | Vue 3.5 호환 확인 |
| tailwindcss | 3.4.19 | ⚠️ 4.3.0 major | v4 마이그레이션 미진행 |
| eslint | 9.39.4 | ⚠️ 10.4.0 major | |
| vite | 7.3.3 | ⚠️ 8.0.14 major | |
| reka-ui | 2.9.7 | ▲ 2.9.8 patch | 8 참조 |

### Rust (apps/desktop/src-tauri/Cargo.toml)
| 패키지 | 현재 | 비고 |
|--------|------|------|
| tauri | 2.1 | ✓ |
| tokio | 1.40 (full) | ✓ |
| git2 | 0.19 | 0.20 체크 권장 |
| sqlx | 0.8 (sqlite) | rusqlite 혼용 없음 (Codex 정정) |
| reqwest | 0.12 (rustls) | ✓ |
| portable-pty | 0.8 | plan/10 integrated terminal |
| keyring | 3.4 | PAT/SSH passphrase |
| encoding_rs | 0.8 | 한글 디코딩 핵심 |
| tempfile | 3.13 | 5건 사용 (한글) |

### Unused 후보 (5-Check, 모두 USER DECISION)
- `notify` (Rust) — plan/04 reservation (v0.x+ .git refs watch). 삭제 금지. **certain reservation**
- `tauri-plugin-fs` — plan/04 reservation 명시. **certain reservation**
- `StatusInlineDiff.vue` / `SyncBar.vue` — Codex flagging, 참조 없음. **uncertain**

### Internal cross-reference Top 5
**Rust**: `error::` (43) > `git::runner::` (31) > `git::path::` (20) > `storage::Db` (11) > `git::repository::` (4)
**Vue**: `vue-i18n` (262) > `@tanstack/vue-query` (80+) > `@codemirror/` (30+) > `reka-ui` (8) > `vue-draggable-plus` (3)

---

## Recommendations (개선 제안)

### HIGH — 즉시 조치 후보

1. **runner.rs:145-164 timeout orphan 해소** — 증거: `apps/desktop/src-tauri/src/git/runner.rs:147` `wait_with_output()` 소유권 이전 후 timeout 시 child kill 불가, 코드 주석에 "best-effort kill 불가, orphan 가능" 자체 명시. / 액션: `tokio::select!` + `tokio::process::Command.spawn()` + `child.kill().await` 패턴으로 재구성, manual stdout/stderr read. / **confidence: certain** (Codex 발견, 6 wave 미해소)

2. **PTY cols/rows 경계 검증 추가** — 증거: `ipc/pty_commands.rs` 의 `PtyOpenArgs { cols: u16, rows: u16 }` 그대로 `PtySize` 전달, 0x0 또는 비정상 크기 방어 없음. / 액션: cols/rows 0 거부 + 상한 클램프 (예: 500×200). / **confidence: certain**

3. **메모리 drift 정정 (god comp 0 → 실측 22)** — 증거: components/ 19 + pages/ 3 = 22건 ≥200 LOC, Sprint c75 마일스톤 메모리와 drift / 액션: (a) 메모리 [sprint_2026_05_12_c75] 업데이트 또는 (b) threshold 200 → 300 재정의 + 신규 god comp 5건 (StatusPanel 670 / CommitGraph 637 / PrDetailModal 523 / GitKrakenToolbar 472 / FullscreenDiffView 433) refactor 결정. / **confidence: certain**

### MEDIUM — 다음 스프린트 후보

1. **forge owner/repo URL 세그먼트 인젝션 방어** — 증거: `forge/*/client.rs` `format!("/repos/{owner}/{repo}/pulls")` 직접 보간. / 액션: `?#%2f` 거부 또는 percent-encode. / **confidence: likely**

2. **importer repo path canonicalize 보강** — 증거: `importer/` 의 `PathBuf::from(stripped).exists()` 후 `starts_with` 검사만, symlink traversal 가능. / 액션: `canonicalize()` 호출 후 workspace 경계 검증. / **confidence: likely**

3. **`#[tracing::instrument]` 도입 확장** — 증거: 현재 6개 (sync_commands 4 + commit_commands 1 + repo_commands 1). 187 IPC 중 3.2% / 액션: HIGH 위험 영역 (bulk_/clone_/fetch_/pull/push/checkout/reset/rebase) 우선 attribute 추가. / **confidence: certain**

4. **vue-query / @codemirror/view / reka-ui patch 업데이트** — 증거: 3 패키지 모두 patch 1 minor 뒤. / 액션: `bun update @tanstack/vue-query@5.100.14 @codemirror/view@6.43.0 reka-ui@2.9.8`. / **confidence: certain**

### LOW — 여유 있을 때

1. **panic_hook 컨텍스트 보강** — 증거: panic_hook.rs payload/file/line 만 로깅 / 액션: spawn 사이트 (`git/bulk.rs:65`, `git/profile_apply.rs:178`, `ipc/pty_commands.rs:145`) wrap 으로 repo_id/session_id/operation context 주입. / **confidence: likely**

2. **god comp pages/ 3건 추출** — 증거: launchpad.vue 554 / repositories.vue 528 / settings.vue 420 / 액션: Pattern 8~14 적용 (use*Actions + use*Mutations 분리). / **confidence: certain**

### User Decision (자동 액션 금지)

1. **`wsl.exe` PTY allowlist 유지 여부** — 증거: `ALLOWED_SHELL_BASENAMES` 에 `wsl.exe` 포함, 명령 분리자 필터 통과해도 WSL 내부 임의 Linux 명령 실행 가능 / 가능 액션: (a) 유지 (의도적 설계) (b) opt-in 토글 도입 (c) 제거 / **confidence: likely** / 권장: 사용자 판단 (직전 sprint 보안 6 wave 의 PTY allowlist HIGH 해소와 정책 정합 재확인)

2. **forge `base_url` scheme 정책** — 증거: keychain 저장 전 scheme/host 검증 없음, `http://attacker.internal` 등록 가능 / 가능 액션: (a) `https://` 만 허용 (b) enterprise 화이트리스트 (c) 유지 (사용자 책임) / **confidence: likely** / 권장: 사용자 판단

3. **`StatusInlineDiff.vue` / `SyncBar.vue` 보존 여부** — 증거: Codex flagging, 참조 없음 / 가능 액션: (a) 유지 (예약 — plan reservation 근거 재확인) (b) 문서화 (c) 제거 / **confidence: uncertain** / 권장: 사용자 판단

4. **docs/solutions/ 카운트 drift** — 증거: 실측 5건 (루트 직접), 메모리 카운트 39 (Sprint 2026-05-26 /analyze) / 가능 액션: (a) 메모리 재측정 (toolkit/global 포함 vs repo only) (b) `node scripts/validate-toolkit.js --quiet` 실행 (toolkit repo 별도) (c) drift 확정 후 메모리 업데이트 / **confidence: likely** / 권장: 사용자 판단

---

## Next Suggestion

| 조건 | 제안 |
|------|------|
| HIGH 3건 + MEDIUM 4건 surface | `/plan runner-timeout-orphan-fix-and-pty-bounds-and-god-comp-refresh` 로 구현 계획 수립 권장 |
| User Decision 4건 (보안 정책 2 + drift 2) | `/research wsl-pty-allowlist-and-forge-base-url-policy` 로 근거 보강 권장 |
| 메모리 drift 2건 (god comp 0 / `#[tracing::instrument]` 0) | `/doc-sync` 또는 메모리 직접 업데이트 권장 |
| validator SoT 미실행 (`scripts/validate-toolkit.js` 본 repo 아님) | toolkit repo (`~/.claude`) 에서 실행 + drift 비교 별도 |

---

> **검증 요약**: 4 agent 중 Codex 가 Claude 단정 2건 (`tracing 0개` / rusqlite 혼용) REJECTED + 신규 HIGH 1건 + MEDIUM 3건 + LOW 1건 발견. Cross-validation 가치 정량 — Codex-only finding 7건 surface 했고 그 중 HIGH-A runner.rs:145-164 는 보안 6 wave 미해소로 다음 sprint 1순위.
