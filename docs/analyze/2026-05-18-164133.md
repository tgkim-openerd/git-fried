# 코드베이스 분석 리포트 — git-fried v0.3.0

> 생성: 2026-05-18 16:41
> 명령: `/analyze`
> 모드: 4 병렬 agent (3 Claude Explore + 1 Codex GPT-5.x Independent)
> Codex parallel agent: **available** (codex-cli 0.130.0)

---

## Rejected Claims (검증 패스 §2.5)

§ 2.5 Verification Pass 의 재실행 결과 다음 sub-agent finding 들을 기각/조정:

| # | Sub-agent claim | 재검증 결과 | 처리 |
| --- | --- | --- | --- |
| R1 | Agent 2 Finding 6: `vue-draggable-plus` 코드 사용 0건, unused 후보 | `CommitGraph.vue:14`, `InteractiveRebaseModal.vue:15`, `RepoTabBar.vue:18` 에서 `import { VueDraggable } from 'vue-draggable-plus'` + template 에서 실 사용 3건 | **REJECTED** — 정상 사용 중 |
| R2 | Agent 2 Finding 13: bun lockfile 부재 | `bun.lock` (텍스트 형식, 1327줄, bun 1.1+ default) 존재 | **REJECTED** — agent 가 구형 `bun.lockb` 파일명만 확인 |
| R3 | Agent 3 Finding 15: ESLint/Prettier "문서 전용", 설정 파일 미발견 | `apps/desktop/.prettierrc.json` + `apps/desktop/eslint.config.js` 실재 | **REJECTED** — agent 가 루트만 grep, workspace 미확인 |
| R4 | Agent 3 Finding 3: `#[tauri::command]` 386개 등록 | `grep -rE "^#\[tauri::command\]" apps/desktop/src-tauri/src --include="*.rs"` 재실행 = **172** | **REJECTED** — agent 가 `docs/analyze/*.md` 내부 인용 패턴까지 포함하여 over-count. Agent 1 의 172 채택 |

---

## Codex Cross-Reference (4번째 병렬 agent — §2.6 cross-validation 결과)

### Agreed (4 agent 일치) — 최고 confidence

- **Vue/Pinia 스택 최신**: Vue 3.5.34, vue-router 4.6.4, Pinia 2.3.1 (Agent 2 + Codex D1 indirect)
- **Tauri Rust ↔ JS minor lag**: Rust `tauri = "2.1"` + plugin `"2.0"` vs JS `@tauri-apps/api "^2.11.0"` + plugin `"^2.4~2.7"` (Agent 2 Finding 2 + Codex D1)
- **`v02_commands.rs` 는 의도된 re-export 모듈, stale 아님**: `pub use crate::ipc::{ai_commands, bisect_commands, lfs_commands, merge_commands, rebase_commands, worktree_commands}::*` 로 lib.rs 의 generate_handler path 보존 (Codex S2, 재확인)
- **PTY shell allowlist 부재 — `is_empty()` 만 검사**: `apps/desktop/src-tauri/src/pty/mod.rs` 의 `pub fn open(...)` 내부 검증 단계 = `if shell.trim().is_empty()` 단 1줄. 임의 binary 실행 가능 (Codex R3, 재확인)

### Claude-only (Codex 미발견 — confidence 1 단계 down)

- **TypeScript `^6.0.3`** 사용 (Agent 2 Finding 3) — `confidence: certain → likely`. Codex 가 별도 surface 안 함. TS 6 은 신규 pre-release 계열이라 추가 모니터링 권고
- **xterm / tempfile / encoding_rs / unicode-normalization** 활성 사용 (Agent 2 Findings 7/9/15)
- **CodeMirror 13 패키지 + vite chunk 분리** (Agent 2 Finding 5)
- **94 Vue 컴포넌트 / 197 composables / 91 test 파일 / 4 page** (Agent 1 + Agent 3) — Codex 가 카운트 따로 안 함, 검증 통과로 유지

### Codex-only (3 Claude agent 미발견 — 추가 5-Check 통과 후 surface)

- **`lib.rs` startup 3 expect() panic path** (lines 70, 73, 262) — 직접 grep 재확인 ✓ (Codex S3, **HIGH 권고 대상**)
- **`branch.rs` / `status.rs` 의 git2 동기 호출이 spawn_blocking 미적용** — `Repository::open()` async context 에서 직접 호출 가능 → Tokio executor block 위험 (Codex P1 + R4)
- **Vue Query 넓은 prefix invalidation** — `['graph']`, `['pr']`, `['repos']` 등 광역 무효화로 관련 없는 캐시 같이 무효화 (Codex P2)
- **`useConfirm` singleton race 의도된 설계** — race condition 아님 (Codex P3, false positive 방어용 정보)

### Disagreements (Codex 가 Claude finding REJECTED)

- 본 회차 Codex 가 직접 REJECTED 한 Claude finding 없음. 대신 Claude 측 4건이 parent context (§2.5) 재검증에서 REJECTED — Rejected Claims 표 참조.

---

## Coverage Audit (§2.7 positive claim verification)

| # | Claim | Depth | Verdict |
| --- | --- | --- | --- |
| 1 | "god comp 0 전체 달성" (MEMORY sprint c75) | L2 | **CONFIRMED** — `node scripts/god-comp-check.mjs` Layer A (script ≥200 LOC) = 0. Layer B (template-heavy total ≥400) = 8 (별도 카테고리, "정상 패턴 / SOT 통합 후보, 강제 추출 아님" 명시) |
| 2 | "i18n ko/en symmetry + 1360 leaf" (MEMORY c95) | L2 | **CONFIRMED** — `node scripts/i18n-leaf-count.mjs` ko=1360 / en=1360 / symmetry=OK |
| 3 | "IPC 172 commands" (Agent 1) | L2 | **CONFIRMED** — `^#[tauri::command]` 재grep 172 일치 |
| 4 | "Pinia 1 store (repos.ts)" (Agent 3) | L2 | **CONFIRMED** — `grep defineStore` 1 hit, `createGlobalState` 호출 0 |
| 5 | "OAuth 미구현" (Agent 3) | L1 | **DOWNGRADED** — keyring + forge_save_token 만 검증, OAuth 코드 부재의 의도성 (예약 vs 미계획) 까지는 미검증. confidence: likely |
| 6 | "ESLint/Prettier 문서 전용" (Agent 3 Finding 15) | L1 | **REJECTED** — actually CONFIGURED (Rejected Claims R3) |
| 7 | "vue-draggable-plus unused" (Agent 2 Finding 6) | L1 | **REJECTED** — actually USED in 3 components (Rejected Claims R1) |
| 8 | "bun lockfile 부재" (Agent 2 Finding 13) | L1 | **REJECTED** — `bun.lock` 텍스트 1327줄 실재 (Rejected Claims R2) |

총 8 단정 중 **CONFIRMED 4 / DOWNGRADED 1 / REJECTED 3**. Coverage Audit 통과율 50% — Claude agent 측 L1 단정의 false positive 가 3건 (REJECTED) 발생, agent 측 5-Check 결락이 원인.

---

## 프로젝트 개요

- **이름**: `git-fried` ([apps/desktop/package.json:2](apps/desktop/package.json#L2) `@git-fried/desktop`)
- **버전**: 0.3.0 (루트 + apps/desktop 양쪽 동일)
- **목적**: GitKraken 대체 desktop git client — Gitea first-class, 한글 안전, Tauri-light, multi-repo native, AI CLI 위임
- **모노레포 구조**: `workspaces: ["apps/*"]` — 현재 단일 워크스페이스 `apps/desktop`
- **프레임워크**: Tauri 2.x (Rust backend) + Vue 3.5 + Vite + Pinia (1 store) + vue-query 5.100 + vue-i18n 11.4 + reka-ui + Tailwind
- **빌드/툴**: bun 1.1+ (workspace), cargo (Rust), vite + vue-tsc + eslint + prettier
- **릴리스 / sprint 상태**: MEMORY 기준 sprint c95+ 종료 (2026-05-18). git log 최근 5 commit 모두 sidebar microgap + GitKraken parity 컨텍스트

---

## 구조 (validator SoT 우선)

| 항목 | 카운트 | 출처 |
| --- | --- | --- |
| Vue 컴포넌트 (components/) | 94 | `find apps/desktop/src/components -name "*.vue"` |
| 페이지 (pages/) | 4 | `find apps/desktop/src/pages -name "*.vue"` |
| Composables | 197 | `find apps/desktop/src/composables -name "*.ts"` (test 제외) |
| 테스트 파일 (.spec.ts/.test.ts) | 91 | `find apps/desktop/src -name "*.spec.ts" -o -name "*.test.ts"` |
| Rust 소스 (.rs) | 88 | `find apps/desktop/src-tauri/src -name "*.rs"` |
| Tauri IPC commands | 172 | `grep -rE "^#\[tauri::command\]"` |
| Pinia store | 1 (`repos.ts`) | `grep -r "defineStore" src/stores` |
| Locale (i18n leaf) | 1360 ko + 1360 en (symmetry OK) | `node scripts/i18n-leaf-count.mjs` |
| docs/solutions | 4 | `find docs/solutions -name "*.md"` |

> **참고**: 프로젝트 루트 `CLAUDE.md` 부재 (글로벌 `~/.claude/CLAUDE.md` 만 존재). MEMORY.md 의 sprint 진행 로그가 사실상 프로젝트 메모 역할.

### 주요 디렉토리 (depth 2)

```
git-fried/
├── apps/desktop/
│   ├── src/
│   │   ├── ai/ api/ components/ composables/
│   │   ├── i18n/ locales/ pages/ stores/
│   │   ├── styles/ types/ utils/
│   │   └── App.vue, main.ts
│   └── src-tauri/
│       └── src/
│           ├── ai/ forge/ git/ importer/
│           ├── ipc/ pty/ ...
│           └── lib.rs, main.rs
├── docs/
│   ├── analyze/ api/ code-review/ design-context/
│   ├── plan/ solutions/ ux-eval/
│   └── IMPLEMENTATION-STATUS.md, QUICK_START.md
├── scripts/
│   └── cargo-rustup.mjs, dev-cleanup.mjs, generate-tauri-commands-index.mjs,
│       god-comp-check.mjs, i18n-leaf-count.mjs, re-verify.mjs, capture-screens.ts
├── package.json (workspace root, v0.3.0)
└── bun.lock (1327 lines, text format)
```

---

## 핵심 패턴

| 영역 | 관찰 사실 |
| --- | --- |
| **상태관리** | `defineStore` 1회 (`repos.ts` composition style). `createGlobalState` 호출 0. 나머지는 composables 내부 `ref/reactive` + 선택적 localStorage 직접 관리. |
| **IPC 추상화** | `invokeWithTimeout` wrapper 196회 사용 — default 30s, long-running (`bulk_*` / `clone_*` / `ai_*`) 별도 prefix policy 적용. Retry/Backoff 명시. |
| **에러 처리** | Rust `AppError { kind, message, stderr, exitCode }` → Vue `formatError() + humanizeGitError()` → toast/notification 3계층. try/catch 176회 across 55 files. 전역 errorHandler 등록. |
| **인증/Forge** | `keyring::Entry` (Rust) 기반 PAT 저장. `forge_save_token` / `forge_list_accounts` / `forge_whoami`. 지원: Gitea + GitHub 만. OAuth 미구현. |
| **컴포넌트 합성** | 94 컴포넌트 / defineProps+defineEmits 112회. provide/inject 6회. slot 패턴 거의 없음. Layer A god comp (script ≥200) **0** (sprint c75 마일스톤 유지). Layer B template-heavy (total ≥400) 8 — 의도된 풍부 UI. |
| **i18n** | `useI18n()` / `$t()` 99 파일에서 사용. 빌드 타임 키 검출 없음 (runtime 에러). |
| **테스트** | vitest 91 .test.ts (composables 중심). Rust inline `#[cfg(test)]` 분산. e2e Playwright 1.59. |
| **Vue Query 캐싱** | `useQuery/useMutation` 306회. 3-tier staleTime (REALTIME 2s / NORMAL 30s / STATIC 60s). Prefix invalidation 광역 (Codex P2). |
| **lint/fmt** | eslint.config.js (flat config) + .prettierrc.json + vue-tsc + cargo fmt/clippy (script: `scripts/cargo-rustup.mjs`). |

### 가장 큰 파일 top 8

```
648  apps/desktop/src/components/StatusPanel.vue        (script 165, template-heavy)
613  apps/desktop/src/components/CommitGraph.vue        (script 199, threshold 직전)
535  apps/desktop/src/pages/launchpad.vue               (script 123)
473  apps/desktop/src/pages/repositories.vue            (script 141)
466  apps/desktop/src/components/PrDetailModal.vue      (script 143)
414  apps/desktop/src/components/GitKrakenToolbar.vue   (script 151)
413  apps/desktop/src/components/FullscreenDiffView.vue (script 176)
399  apps/desktop/src/components/CommitDetailSidebar.vue (script 140, total 400)
```

> **참고**: `CommitGraph.vue` 의 script 199 LOC 는 Layer A threshold (200) 직전 — composable 추출 여유 1줄. 다음 변경 시 자동 god 회귀 위험.

---

## 의존성

### Frontend (apps/desktop/package.json)

| 카테고리 | 패키지 | 버전 | 비고 |
| --- | --- | --- | --- |
| Vue 코어 | vue, vue-router, pinia | 3.5.34 / 4.6.4 / 2.3.1 | 모두 최신 stable |
| Vue 부속 | vue-i18n, @tanstack/vue-query, @tanstack/vue-virtual | 11.4.2 / 5.100.10 / 3.13.24 | 적극 사용 |
| Tauri JS | @tauri-apps/api, @tauri-apps/cli | ^2.11.0 / ^2.11.1 | Rust 와 minor 차이 |
| Tauri plugin JS | plugin-deep-link/dialog/fs/notification/shell | 2.4.9~2.7.1 | 산포 |
| Editor | @codemirror/* 13개, @xterm/* 3개 | 6.x / 5.x | vite chunk 분리 |
| Font | @fontsource-variable/roboto-flex, @fontsource/noto-sans-kr | 5.x | 한글 안전 |
| UI | reka-ui, vue-draggable-plus | 2.9.7 / 0.6.1 | draggable: 3 컴포넌트 사용 |
| Lang | typescript | ^6.0.3 | **pre-release 계열, 모니터 필요** |
| 기타 | tailwindcss, vite, vitest, playwright, eslint, prettier | latest stable |

### Backend (apps/desktop/src-tauri/Cargo.toml)

| 카테고리 | crate | 버전 | 비고 |
| --- | --- | --- | --- |
| Tauri | tauri | 2.1 (features=[]) | JS minor (2.11) 와 lag |
| Tauri plugins | plugin-deep-link/dialog/fs/notification/shell | 2.0 | JS minor (2.4~2.7) 와 lag |
| Git | git2 | 0.19, vendored-libgit2 + vendored-openssl | 핵심, 정적 링크 |
| Async | tokio, async-trait | 1.x / 0.1 | spawn_blocking 일부 경로만 (Codex P1) |
| DB | sqlx | latest, sqlite feature | profiles/forge accounts seed |
| Auth | keyring | latest | PAT 저장 |
| 로깅 | tracing, tracing-subscriber | 0.1 / 0.3 env-filter | `#[instrument]` IPC 다수 |
| 한글 | encoding_rs, unicode-normalization | 0.8 / 0.1 | 한글 안전의 핵심 |
| 예약 (미사용) | notify, tauri-plugin-fs | 6.1 / 2.0 | plan/04 file-system-watch 로드맵, c53에서 capability 차단 |
| 테스트 | tempfile | 3.13 | git/rebase/worktree 테스트 |

### 의존성 정합

- **lockfile**: `bun.lock` (1327 lines, text format, bun 1.1+) + `apps/desktop/src-tauri/Cargo.lock` (190KB, 2026-05-15) 모두 정합. frozen install 가능.
- **git+https URL 의존성**: package.json 검사 결과 git+ scheme 의존성 0건 — § bun + git URL SHA bump 함정 적용 대상 아님.
- **transitive depth**: CodeMirror 13 + Tauri 5 plugin + xterm 3 + vue-query/virtual = 큰 dep 트리 (lockfile 1327줄 반영).

---

## Recommendations

### HIGH — 즉시 조치 후보

1. **lib.rs 3 expect() panic path → tauri::Builder.setup() Result + dialog** (Codex R1 / S3)
   - 증거: [apps/desktop/src-tauri/src/lib.rs:70](apps/desktop/src-tauri/src/lib.rs#L70), [L73](apps/desktop/src-tauri/src/lib.rs#L73), [L262](apps/desktop/src-tauri/src/lib.rs#L262)
   - 액션: tokio runtime build / AppState init 실패를 `tauri::Builder::setup` 에서 `Err` 반환 + `tauri-plugin-dialog` 으로 사용자 알림 후 종료. Tauri 2.x setup hook 이 `Result` 반환 지원.
   - confidence: **certain** (Agreed — 직접 grep 으로 3 라인 확인)

2. **PTY shell allowlist 부재 — 임의 binary 실행 가능** (Codex R3)
   - 증거: [apps/desktop/src-tauri/src/pty/mod.rs](apps/desktop/src-tauri/src/pty/mod.rs) `pub fn open(&self, cwd, shell, ...)` 내부 검증 = `if shell.trim().is_empty()` 1줄만
   - 액션: `["pwsh", "powershell", "cmd", "bash", "sh", "zsh", "fish"]` 화이트리스트 + 미일치 시 `AppError::validation` 반환. 추가로 절대 경로 path traversal 차단.
   - confidence: **certain** (Agreed — pty/mod.rs 직접 read 로 확인)

### MEDIUM — 다음 스프린트 후보

1. **Tauri Rust/JS minor 버전 정렬** (Codex R2 + Agent 2 Finding 2)
   - 증거: `Cargo.toml` `tauri = "2.1"` + `tauri-plugin-* = "2.0"` vs `package.json` `@tauri-apps/api ^2.11.0` + plugin-* `2.4~2.7`
   - 액션: `tauri = "2"` (permissive) 또는 `"2.11"` 으로 bump + `cargo update` + `cargo test` + smoke e2e.
   - confidence: **likely** (Agreed)

2. **`branch.rs` / `status.rs` 의 git2 동기 호출 → spawn_blocking 래핑** (Codex P1 + R4)
   - 증거: [apps/desktop/src-tauri/src/git/branch.rs](apps/desktop/src-tauri/src/git/branch.rs) L33, [status.rs](apps/desktop/src-tauri/src/git/status.rs) L60/L97 — `Repository::open()` 직접 호출. `bulk.rs:207/248` 의 `spawn_blocking` 패턴 미적용
   - 액션: 해당 git2 blocking 함수 호출부를 `tokio::task::spawn_blocking(move || { ... }).await?` 로 래핑. `bulk.rs` 참고.
   - confidence: **likely** (Codex-only)

3. **Vue Query 광역 prefix invalidation 검토** (Codex P2)
   - 증거: [useHiddenRefs.ts:52](apps/desktop/src/composables/useHiddenRefs.ts#L52), [usePrMutations.ts:120-122](apps/desktop/src/composables/usePrMutations.ts#L120), [Sidebar.vue:46-49](apps/desktop/src/components/Sidebar.vue#L46) — `['graph']`, `['pr']`, `['repos']` 같은 짧은 prefix 다수
   - 액션: 의도된 광역 invalidation 인지 audit. 의도된 경우 주석 추가, 의도 외 경우 더 specific key (`['graph', repoId]`) 로 좁히기.
   - confidence: **likely** (Codex-only)

### LOW — 여유 있을 때

1. **TypeScript ^6.0.3 모니터링** (Agent 2 Finding 3)
   - 증거: [apps/desktop/package.json](apps/desktop/package.json) `"typescript": "^6.0.3"`
   - 액션: TS 6.x release notes 모니터 + vue-tsc 호환성 주기 확인 (현재 빌드 통과 시 즉시 조치 불필요).
   - confidence: **likely** (Claude-only, Codex 미지적 → uncertain 한 단계 down)

2. **`CommitGraph.vue` script 199 LOC — Layer A threshold (200) 직전**
   - 증거: `node scripts/god-comp-check.mjs` 출력
   - 액션: 다음 수정 시 +1 LOC 만으로 god 회귀. 미리 1~2 composable 추출 (예: `useCommitGraphInteraction`).
   - confidence: **certain**

### User Decision (자동 액션 금지)

> Negative assertion / destructive follow-up 후보. 사용자 판단 영역.

- **OAuth 미구현** (Agent 3 Finding 5)
  - 증거: `grep "OAuth" apps/desktop/src-tauri/src/auth.rs` = 0 hit, `keyring::Entry` 사용 1 hit
  - 가능 액션 3개: (a) 유지 — PAT 만으로 충분 / (b) 문서화 — README/CLAUDE 에 "OAuth 미지원" 명시 / (c) plan 작성 — GitLab 추가 시 OAuth 필요 가능성
  - confidence: likely / 권장: 사용자 판단

- **`notify` / `tauri-plugin-fs` 예약 의존성** (Codex D2 + Agent 2 Finding 8)
  - 증거: `Cargo.toml:54-59` 주석 + `CHANGELOG.md:83` "plan/04 file-system-watch reservation" + 실제 코드 0 import
  - 가능 액션 3개: (a) 유지 — 의도된 예약 / (b) plan/04 진행 일자 명시 후 유지 / (c) plan/04 폐기 시 제거
  - confidence: certain (예약 의도 확인됨) / 권장: 유지

---

## Next Suggestion Rules

| 조건 | 제안 |
| --- | --- |
| HIGH 2건 + MEDIUM 3건 존재 | `/plan rust-startup-hardening` 으로 HIGH 1+2 (lib.rs expect + PTY allowlist) 동시 구현 계획 권고 |
| User Decision 2건 존재 | `/research oauth-vs-pat-strategy` 로 OAuth 도입 여부 근거 보강 권고 |
| 프로젝트 루트 CLAUDE.md 부재 (MEMORY 만 존재) | `/doc-sync` 로 CLAUDE.md 신규 생성 권고 — 현재 sprint c95+ 컨텍스트 + 모듈 catalog 반영 |
| MEMORY 카운트가 validator SoT 와 drift | 본 회차 측정 기준 동기화: composables 197 / 컴포넌트 94 / IPC 172 / i18n 1360 / Pinia 1 (MEMORY 의 일부 카운트 stale 가능 — 다음 `/doc-sync` 에서 갱신) |

---

## Obsidian Integration

```bash
node -e "require(require('path').join(require('os').homedir(),'.claude/scripts/obsidian-log')).logToObsidian('analyze', {project:'git-fried', details:'파일:88 Rust + 94 Vue + 197 composables, 의존성:55+, 개선 제안:5 + User Decision 2', context:{what:'코드베이스 구조/의존성/패턴 4 병렬 분석 (3 Claude + 1 Codex)', why:'sprint c95+ 종료 후 다음 sprint 진입 전 substrate 파악', how:'Explore 3 + codex-rescue 1 병렬 + §2.5 verification + §2.6 cross-validation + §2.7 coverage audit'}})"
```
