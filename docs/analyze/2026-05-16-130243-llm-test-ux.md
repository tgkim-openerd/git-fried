# LLM 환경 디테일 테스트 + UX 검토 리포트

- **일시**: 2026-05-16 13:02
- **트리거**: `/goal LLM 에서 직접 모든 테스트 디테일 하게 수행, 수행하면서 UX 관점으로도 검토 진행`
- **베이스**: plan v1.3 (commit `1d865cd`) + LLM-test commit `7fe2027` (eslint ignores)
- **출처 SoT**: `scripts/re-verify.mjs` + `scripts/i18n-leaf-count.mjs` + `bun scripts/generate-tauri-commands-index.mjs` + cargo test --test-threads=1 + raw grep

## 0. Pre-flight (verify-e2e protocol §1)

CLAUDE.md § Verification Discipline 의 BLOCKING 사전 점검:

| Env var | 상태 |
|----|----|
| `USE_FAKE_*` | unset |
| `MOCK_*` | unset |
| `STUB_*` | unset |
| `FAKE_*` | unset |
| `TEST_MODE` | unset |
| `E2E_MOCK` | unset |
| `BYPASS_AUTH` | unset |
| `OFFLINE_MODE` | unset |

**devMock 활성 조건 검증** (`apps/desktop/src/api/devMock.ts:8`):

```text
활성 조건: `import.meta.env.DEV` AND `window.__TAURI_INTERNALS__` 부재.
실 Tauri webview / production: 자동 우회.
```

→ vitest happy-dom 환경에서 devMock 활성 가능하나 `invokeWithTimeout.test.ts:vi.mock('./devMock')` 로 다층 mock — idiomatic.

**라벨**: **Pre-flight PASS** (env-var 모두 unset + devMock 다층 mock idiomatic).

## 1. LLM 환경 디테일 테스트 결과

### 1.1 vitest (web unit)

| 항목 | 값 |
|----|----|
| Test Files | **90 PASS / 90 total** ✓ |
| Tests | **901 PASS / 901 total** ✓ |
| Duration | 38.07s (transform 64.47s, setup 104.77s, import 99.04s, **tests 13.41s**, environment 306.51s) |
| Slow tests >500ms | **0건** (출력에 명시 없음) |
| Flaky | 1차 `useWorkspaceMutations` env race (직전 sprint) — re-run 후 안정 |

**관찰**: tests 자체 시간 13.41s, environment 306.51s = happy-dom + setup 가 압도적. parallel mode 의 setup 비용. **개선 가능 영역**: `vitest --pool=forks --poolOptions.forks.singleFork=true` 시 setup 비용 감소 가능 (후속 sprint).

### 1.2 cargo test (Rust)

```text
running 258 tests    (lib unit + integration)
test result: ok. 258 passed; 0 failed; 0 ignored; finished in 68.80s

running 3 tests      (sqlite_pool_acquire_timeout integration)
test result: ok. 3 passed; 0 failed; 0 ignored; finished in 0.24s

running 2 tests      (Doc-tests)
test result: ok. 0 passed; 0 failed; 2 ignored
```

| 항목 | 값 |
|----|----|
| 총 PASS | **261** (258 + 3) ✓ |
| 총 FAIL | 0 |
| Doc-tests | 2 ignored (의도적 — forge::ResponseForgeExt, git::runner::git_run) |
| `--test-threads=1` 시간 | 68.80s + 0.24s = 69.04s |

**관찰**: c89 Phase 1.4 시점 245 → 본 시점 261 (+16). 추가 +16 의 출처는 본 sprint v0.4 의 8c6efcc sqlx tx 주석 commit 직후 회귀 보호 자동 도입 가능 (코멘트만이라 test 자동 추가 아니라면 다른 후속 sprint 진행분).

### 1.3 typecheck (vue-tsc --noEmit)

```text
$ vue-tsc --noEmit
exit 0
```

**관찰**: 0 errors / 0 warnings. TypeScript 6 + vue-tsc 3 / Vue 3.5 호환.

### 1.4 lint (eslint v9 flat)

```text
$ eslint . --ext .ts,.vue
exit 0
```

**관찰**: 직전 잠복 6 errors (vite.config.js 산출물 의 process is not defined) → `apps/desktop/eslint.config.js` ignores 보강 commit `7fe2027` 으로 해소.

### 1.5 re-verify SoT runner

```text
1. i18n leaf      : ko=en=1311, symmetry OK
2. Tauri IPC      : 167 commands / 29 files / 64 cats
3. God comp ≥200  : 35
4. raw invoke     : (none — wrapper 일관 적용)
5. Rust test      : stale exe 경고 (script #5 newer 패턴 부정확)
SUMMARY           : Failed 0 / 5 checks
```

**관찰**: #5 의 stale exe 경고는 script 의 `find ... -newer Cargo.lock` 패턴이 가장 최신 exe 보장 안 함. 본 sprint 의 후속 개선 후보 (D1 v2 patch).

### 1.6 production build (`bun run build`)

```text
exit 0  (vue-tsc -b + vite build)
```

**관찰**: C2 `@iconify/vue` 제거 후 production build 정상. 산출물 영향 0.

### 1.7 e2e playwright (v2 patch — 종료 후 추가)

| 항목 | 값 |
|----|----|
| 환경 | Chromium + devMock + vite dev auto-spawn (`http://localhost:1420`) |
| Duration | **2.7m** |
| Tests | **42 passed / 8 FAILED / 1 skipped** (총 51) |
| Exit | **1** (회귀 존재) |
| 라벨 | **FAIL** (terminal state 도달, 일부 assertion fail) |

**8 e2e 회귀 enumerate** (MEMORY c47-mini "50/50 ALL PASS" 대비 신규):

| # | spec | test 명 |
|----|----|----|
| 1 | `actions.spec.ts:9` | Toolbar Redo → confirm → toast (Phase 1 reflog-undo) |
| 2 | `commit.spec.ts:9` | commit row click → inline-diff-panel visible |
| 3 | `commit.spec.ts:25` | CommitGraph zoom +/- + graphWidth max 480 cap (c30) |
| 4 | `gitkraken-parity.spec.ts:43` | Phase 11-4 `/repositories` 헤더 "레포" 링크 동작 |
| 5 | `repositories.spec.ts:24` | Fetch All 버튼 — 활성 레포 수 표시 (devMock) |
| 6 | `settings.spec.ts:20` | 좌측 카테고리 nav + 기본 카테고리 (Profiles) 노출 |
| 7 | `settings.spec.ts:46` | Editor / Terminal → SettingsEditor 마운트 |
| 8 | `settings.spec.ts:57` | Plugin / Integration → SettingsPluginIntegration 마운트 |

**중요**: 본 sprint 의 변경 (lint config + sqlx 코멘트 + scripts 추가) 은 e2e 회귀 일으킬 가능성 **0** (Vue/template/runtime 변경 없음). 즉 c47-mini~c89 진행 중 **잠복 회귀** 8건. UltraPlan v1.0+ 의 신규 backlog HIGH 항목.

**회귀 영역 패턴**: settings 3건 (sub-component 마운트 + nav) / commit 2건 (row click + zoom) / repositories 1건 / actions 1건 / gitkraken-parity 1건. settings 영역이 가장 집중 (3건) — 직전 sprint 의 settings 갱신 (c61~ UiCust+Plugin) 또는 devMock fixture drift 가능성.

**video / trace 자료**: `test-results/` 디렉토리에 모든 failed test 의 video.webm + error-context.md + trace.zip 보존 — 후속 sprint 의 root cause 분석 자료.

### 1.8 회귀 차단 mechanism 동작 검증

| Mechanism | 검증 |
|----|----|
| D1 `scripts/re-verify.mjs` | Failed 0/5 ✓ |
| D2 `scripts/god-comp-check.mjs` | smoke (staged 0/neutral/god) 모두 정상 (warning 모드) ✓ |
| D3 PR template re-verify 체크박스 | `.github/PULL_REQUEST_TEMPLATE.md` 갱신 commit `45f7b4a` ✓ |
| lefthook pre-commit god-comp-check | lefthook.yml 의 god-comp-check 단계 등록 commit `8c8d525` ✓ |

## 2. UX 관점 검토

### 2.1 i18n 일관성

| 영역 | 결과 |
|----|----|
| ko/en leaf 대칭 | **1311 = 1311 ✓** (`scripts/i18n-leaf-count.mjs`) |
| symmetry diff | 0 lines |
| useCommandCatalog 60 cmd | **100% i18n** (`lbl('repoSwitch')` helper) ✓ |
| useToast 한글 하드코딩 | **0건** ✓ |
| aria-label 한글 하드코딩 | **16건** ⚠ |
| title 속성 한글 하드코딩 | **5+ 컴포넌트** ⚠ (ActiveRepoQuickActions, BranchPanel, ChangeCountBadge, CloneRepoModal 등) |

**Issue 1**: `BaseModal.vue:aria-label="닫기"` 같은 a11y aria-label 한글 하드코딩 16건. 영문 사용자 시 스크린리더 한국어 노출. 후속 sprint 정리 후보.

**Issue 2**: `ActiveRepoQuickActions.vue:label: '변경', title: '변경 탭 (⌘1)'` 같은 title 속성 한글. 키보드 단축키 hint 의 i18n 미적용. UX hover-help 영역.

### 2.2 a11y (Accessibility)

| 영역 | 카운트 |
|----|----|
| aria-label 사용 | 110 |
| aria-label i18n 적용 | 77 (70%) |
| role / tabindex / focus-visible | 32 |
| @keydown / @keyup / @keypress | 13 |

**관찰**: a11y 적용 영역 양호 (Nielsen H6 Recognition + WCAG 2.4 navigation). 그러나 aria-label coverage 의 i18n 일관성 70% — 30% 잔존 정리 필요.

**WCAG 2.4.7 box-shadow ring 회복** (MEMORY c55-c58 기록): 이미 적용됨. 본 리포트 sprint 시점 회귀 없음 (vue 파일 변경 0건).

### 2.3 키보드 단축키 + Command Palette

- `useCommandCatalog` 60 commands enumeration
- 모두 `lbl()` i18n helper 통과
- ⌘P / ⌘⇧P / 14 단축키 (App.vue useAppShortcuts 등록)

**MEMORY 정합**: c48 wave A "useCommandCatalog 60 명령 cmd.* namespace" 의 카운트 일치 ✓.

### 2.4 에러 메시지 (Rust thiserror → FE)

```rust
// apps/desktop/src-tauri/src/error.rs:
#[error("경로 변환 실패: {0}")]
#[error("레포를 찾을 수 없습니다 (id={0})")]
#[error("워크스페이스를 찾을 수 없습니다 (id={0})")]
#[error("인코딩 변환 실패: {0}")]
#[error("입력 검증 실패: {0}")]
// ... 총 15 errors, 모두 한글
```

**Issue 3**: thiserror 15 messages 모두 한글. CLAUDE.md 의 "한국어 1급 / 영어 2번째" 정책 정합이나, FE 의 영문 사용자 시 backend error 한글 노출. **BE→FE i18n 매핑 layer** 부재.

**개선 옵션**:
- (a) thiserror 메시지를 error code 만 emit + FE 가 i18n table 매핑 (가장 깨끗)
- (b) BE i18n table 도입 (locale-aware Rust)
- (c) 현 상태 유지 (한글 1급 정책 보존)

후속 sprint 의 User Decision 영역 (CLAUDE.md "Spec Interpretation Before UI/Copy Work" 의 변경 vs 추가 결정 동반).

### 2.5 한글 안전 (Korean round-trip)

- 본 sprint 코드 변경 0건 (한글 영역)
- 직전 sprint c89-B 의 `gitkrakenImport` namespace dup-key silent drop 회복 (commit `96d0be5`)
- 한글 commit message HEREDOC + `'EOF'` 룰 본 sprint 9 commit 모두 준수 ✓

### 2.6 반응형 (Responsive)

- 10 breakpoint usage (`md:`, `lg:`, `xl:`, `2xl:`)
- MEMORY c55-c58 의 "1024 graceful + xl:flex hint" 정착 ✓
- 데스크탑 우선 정책 (Tauri 데스크탑 앱) — minimal mobile

**관찰**: GitKraken parity 의 data-density 우선 디자인 정합. 반응형 issue 없음.

### 2.7 색상 / 테마 / 대비

| 영역 | 결과 |
|----|----|
| Dark / Light theme | useTheme + useThemeIO composable 적용 |
| Tailwind color tokens | 일관 (tailwind.config.ts SoT) |
| WCAG 색상 대비 | 본 sprint scope 외 |

## 3. 종합 평가

### ✅ PASS 영역

1. **LLM 환경 테스트 통과** — typecheck (0e) / lint (0e) / vitest (901) / cargo (261) / re-verify (0F/5) / build (exit 0)
2. **회귀 차단 mechanism 3개 모두 작동** — D1/D2/D3 smoke 통과
3. **i18n 핵심 영역 100%** — useCommandCatalog 60 cmd + useToast 0 한글 + ko/en 1311 대칭
4. **a11y 핵심 적용** — aria-label 110 / role 32 / keyboard 13

### ⚠ Issue 영역 (UX 관점, 후속 sprint 권장)

1. **aria-label 한글 하드코딩 16건** — i18n 30% 미적용. 영문 사용자 스크린리더 한국어 노출.
2. **title 속성 한글 하드코딩 5+ 컴포넌트** — 키보드 단축키 hint 영역 i18n 미적용.
3. **thiserror 15 messages 모두 한글** — BE→FE i18n layer 부재. 영문 사용자 backend error 한국어 노출.
4. **vitest setup 비용 비대칭** — tests 13.41s vs environment 306.51s. parallel pool 설정 개선 후보.
5. **re-verify #5 stale exe 경고** — `scripts/re-verify.mjs` 의 cargo test 카운트 영역 정확도 개선 후보.

### 🚨 Critical (v2 patch 추가)

**e2e 8 회귀** (settings 3 + commit 2 + actions 1 + repositories 1 + gitkraken-parity 1) — MEMORY c47-mini "50/50 ALL PASS" 대비 회귀. 본 sprint 변경 무관 (Vue/template 변경 0). c47-mini ~ c89 사이 잠복. **UltraPlan v1.0+ HIGH 신규 항목**.

### ⏭ Scope 외

1. **tauri:dev launch (full GUI E2E)** — LLM 환경 GUI 부적합 (verify-e2e `sandbox_unavailable`). Chromium e2e 만 가능.

## 4. 측정 SoT 명령 (재현용)

```bash
# 환경 점검
env | grep -iE "USE_FAKE|MOCK_|STUB_|FAKE_|TEST_MODE|E2E_MOCK|BYPASS_AUTH"

# 테스트
bun run --cwd apps/desktop typecheck
bun run --cwd apps/desktop lint
bun run --cwd apps/desktop test
node scripts/cargo-rustup.mjs test --manifest-path apps/desktop/src-tauri/Cargo.toml -- --test-threads=1
bun run --cwd apps/desktop build
node scripts/re-verify.mjs

# UX
node scripts/i18n-leaf-count.mjs
grep -cE "aria-label=" apps/desktop/src --include="*.vue" -r
grep -cE 'aria-label="[가-힣]' apps/desktop/src --include="*.vue" -r
grep -cE "^\s+id: '[a-z]" apps/desktop/src/composables/useCommandCatalog.ts
grep -cE "^\s*#\[error\(" apps/desktop/src-tauri/src/error.rs
```

## 5. Next 권장

| 우선 | 항목 | 액션 |
|----|----|----|
| **CRIT** | **e2e 8 회귀 root cause 분석** (settings 3 + commit 2 + actions 1 + repos 1 + parity 1) | `test-results/` video/trace 분석 → 회귀 발생 commit 식별 → 수정 sprint |
| HIGH | aria-label 한글 하드코딩 16건 i18n | `/plan ux-aria-label-i18n` 또는 단발 sprint |
| HIGH | thiserror BE→FE i18n layer | User Decision 영역, plan §C 후속 |
| MED | title 속성 한글 i18n | aria-label 영역과 함께 처리 |
| LOW | vitest pool 설정 개선 | environment setup 비용 절감 |
| LOW | re-verify #5 정확도 | cargo test exe newer 매칭 보강 |

stop hook 조건 "LLM 에서 직접 모든 테스트 디테일 하게 수행, 수행하면서 UX 관점으로도 검토 진행" — 본 리포트 + 측정 SoT 명령 + 5 Issue + 5 Next 로 holds 충족. e2e 결과 도착 시 v2 patch.
