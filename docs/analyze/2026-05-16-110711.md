# 코드베이스 분석 리포트 — git-fried v0.3.0

- **분석 일시**: 2026-05-16 11:07
- **분석 방식**: 3 Claude Explore agent (Structure / Dependency / Pattern) 병렬 + 1 Codex Independent agent (background, 결과 미도착 시점)
- **Verification**: 핵심 finding 13건 parent context 재실행 검증 — 일부 sub-agent 카운트 REJECT / INCONCLUSIVE 발생

---

## Rejected Claims (Verification Pass — §2.5)

Sub-agent 가 emit 한 finding 중 parent 재검증에서 일치하지 않은 항목:

| # | Sub-agent claim | 재실행 결과 | Verdict |
|---|----|----|----|
| 1 | Agent 1: "god component 30개 (>=200 LOC .vue)" | 실측 **35개** (`find ... -exec wc -l + awk '$1>=200'`) | **REJECTED** — 실측 35 채택 |
| 2 | Agent 3: "god component 31개" | 실측 **35개** | **REJECTED** — 실측 35 채택 |
| 3 | Agent 1: "composable 146개" | 실측 **124개** (test 제외) | **REJECTED** — 실측 124 채택 (Agent 1 은 .test.ts 포함한 듯) |
| 4 | Agent 1: "Rust #[test] 167개" | 실측 **259개** (`#[test]` + `#[tokio::test]` + `#[sqlx::test]`) | **INCONCLUSIVE** — Agent 1 은 `#[test]` 만 카운트, methodology 차이 |
| 5 | Agent 3: "vue-query useQuery 185회" | 실측 **57회** | **REJECTED** — Agent 3 카운트 과대 |
| 6 | Agent 3: "useMutation 110회" | 실측 **108회** | **CONFIRMED (근접)** |
| 7 | Agent 3: "invoke 직접 호출 196회" | 실측 **172회** | **INCONCLUSIVE** — regex 차이로 ±15% 변동 가능 |
| 8 | Agent 3: "i18n leaf 1311 (대칭 검증 필요)" | `node scripts/i18n-leaf-count.mjs` → ko/en 각 **1311**, symmetry **OK** | **CONFIRMED** |

> SoT 룰: i18n leaf count 는 `scripts/i18n-leaf-count.mjs` 가 SoT, 위 결과 채택.

---

## Codex Cross-Reference (4번째 병렬 agent — §2.6)

> **Codex parallel agent: completed** (task-mp7pl849-cufl1m, agentId ae92793e1cce41950, elapsed **14m 58s**, status 0, threadId `019e2e8d-6266-7011-a683-834f0387cadd`).

Codex 가 emit 한 6 finding (Structure 2 + Dependency 1 + Pattern 2 + Recommendations 1) 을 parent 가 4 분류 cross-validation:

### Agreed (4 agent 일치)

> 없음 — Claude 3 agent 와 Codex 가 서로 다른 영역에 집중. Claude 는 카운트/구조에, Codex 는 stale catalog + tx guard + 단일 dead package 발견에 집중. **상보 관계**로 작동.

### Claude-only (Codex 미언급)

다음 Claude finding 들은 Codex 가 다루지 않음 — confidence 1 단계 down (CLAUDE.md §2.6 룰):

- God component 35개 (StatusPanel 648 / CommitGraph 613 …) — `certain` → `likely`
- invoke 직접 172 vs wrapper 263 (수렴 진행) — `likely` → `uncertain`
- vue-query hook 165 (useQuery 57 + useMutation 108) — `certain` → `likely`
- i18n leaf 1311 ko=en 대칭 OK — `certain` 유지 (script SoT 출력은 down 예외)

### Codex-only (3 Claude agent 미발견 — 5-Check 통과 시 surface)

다음 3 finding 모두 parent 5-Check 재검증 **CONFIRMED**:

1. **Tauri IPC catalog drift** — claim: `#[tauri::command]` 코드 실측 vs `docs/IMPLEMENTATION-STATUS.md` 19행 catalog stale.
   - verification: `grep -rE "^\s*#\[tauri::command\]" apps/desktop/src-tauri/src --include="*.rs" | wc -l` → **171** (Codex 173 ≈ 실측 171, regex 차이) + `find apps/desktop/src-tauri/src/ipc -name "*.rs" | wc -l` → **29**
   - SoT 명시 (catalog): "169 등록 / 26 파일 / 66 카테고리"
   - **drift**: +2 command 등록, +3 ipc 파일 (c46+ 이후 추가분 미반영)
   - verdict: CONFIRMED / confidence: certain
   - 권고: `bun scripts/generate-tauri-commands-index.mjs` 재생성 + IMPLEMENTATION-STATUS.md 19행 갱신.

2. **sqlx tx rollback guard 부재** — claim: 2개 tx site 모두 explicit `tx.rollback()` 없음.
   - verification: `grep -rnE "pool\.begin\(|tx\.commit\(\)|\.rollback\(\)" apps/desktop/src-tauri/src --include="*.rs"` → `profiles.rs:160 begin / :170 commit` + `git/hide.rs:127 begin / :149 commit` (rollback 라인 0)
   - verdict: CONFIRMED / confidence: likely
   - 주의: sqlx 의 `Transaction` Drop trait 가 자동 rollback 호출 → idiomatic 일 수도. 단, panic boundary + cancellation safety 측면에서 명시적 `?` 후 컨트롤 흐름 점검 가치 있음 (특히 `git/hide.rs:127~149` 의 begin~commit 사이 ~22 LOC 처리 구간).
   - 권고: MED — 두 site 의 panic/cancellation 경로 review (자동 rollback 으로 충분한지 확인 후 문서화 또는 explicit guard 추가).

3. **@iconify/vue dead candidate** — claim: `apps/desktop/package.json:56` 의 `@iconify/vue@^4.3.0` 가 코드 0 hit.
   - verification: `grep -rE "@iconify/vue|Iconify|<Icon\b" apps/desktop/src --include="*.ts" --include="*.vue" | wc -l` → **0**
   - 5-Check 결과: Glob OK / Read OK / settings hit 0 / test hit 0 / docs hit 1 (`IMPLEMENTATION-STATUS.md:304` 의 dead 추정 코멘트 자체)
   - verdict: CONFIRMED / confidence: certain
   - **Claude Agent 2 의 "dead 후보 없음" 단정과 충돌** — Agent 2 가 false negative. 본 항목은 **destructive follow-up (제거)** 동반이라 User Decision 블록 배치.

### Disagreements (Codex 가 Claude REJECTED)

- **"AI subprocess read_to_end OOM" 가정 REJECTED** — Codex 가 보편적 우려 자체를 검증 후 reject. 본 의심은 Claude 3 agent 출력에 명시되지 않았음 (Codex 가 자체 가설 검증). `apps/desktop/src-tauri/src/ai/runner.rs:170~193` 에 `stdout.take(AI_RUN_MAX_OUTPUT_BYTES)` + `stderr.take(...)` + `timeout(...)` + `child.kill().await` + `stdout_task.abort()` 모두 구현 확인 — c82 SAF-401 작업 성과 재확인 (CLAUDE.md MEMORY c82 sprint 항목과 일치).

### Codex 의 c89 Backlog cross-check 결과

- **FullscreenDiffView SOT (dce39f8)**: `1 file changed, 18 insertions(+), 49 deletions(-)` — Codex CONFIRMED certain
- **StatusTreeDirRow SOT (886016d)**: `2 files changed, 85 insertions(+), 42 deletions(-)`, `<StatusTreeDirRow` 사용 4건 — Codex CONFIRMED certain
- **Tauri Phase 3 Option C** (tauri.conf.json:25 `additionalBrowserArgs: ""`): schema 호환만 verify, 실 CDP attach 미검증 — plan/36:375 체크리스트에 명시됨 — Codex CONFIRMED certain
- **Phase 2 Option B**: Windows DLL `STATUS_ENTRYPOINT_NOT_FOUND` 로 중단 (Cargo.toml:93 코멘트 보존) — Codex CONFIRMED certain
- **i18n dup-key 검출 (70e6ea9)**: `scripts/i18n-leaf-count.mjs` 에 raw regex 추가 — Claude 측에서도 확인 완료, Agreed.

---

## 프로젝트 개요

| 항목 | 값 |
|----|----|
| **이름** | git-fried |
| **버전** | 0.3.0 (private, MIT) |
| **설명** | GitKraken 대체 Tauri 데스크탑 git 클라이언트 — Gitea 1급, 한글 안전, 멀티 repo |
| **구조** | 모노레포 (`workspaces: ["apps/*"]`), 현재 단일 app `apps/desktop/` |
| **프레임워크** | Vue 3.5.34 + Vite 7.3.3 + Pinia 2.3.1 + vue-router 4.6.4 + vue-i18n 11.4 + vue-query 5.100 |
| **데스크탑** | Tauri 2.11 + Rust edition 2021 (rust-version 1.77) |
| **스타일** | Tailwind CSS 3.4.19 + reka-ui 2.9.7 + @fontsource Roboto Flex / Noto Sans KR |
| **API** | Tauri IPC (Rust commands ↔ frontend invoke), 외부 OpenAPI: Gitea + GitHub REST (reqwest) |
| **DB** | sqlx 0.8 SQLite (runtime-tokio, migrate, chrono) |
| **에디터** | CodeMirror 6 (lang × 6 + theme-one-dark + merge) |
| **터미널** | @xterm/xterm 5.5 + portable-pty 0.8 |
| **패키지매니저** | bun ≥1.1.0 |
| **최근 커밋** | `c3c4b46` — docs(plan/36) v0.3 patch Backlog 1~4 자율 진행 완료 |

---

## 구조

### 디렉토리 트리 (실측)

```
git-fried/
├── apps/desktop/                  # 단일 Tauri app
│   ├── src/                       # Vue 3 프론트엔드 (TS 233, .vue 98)
│   │   ├── components/            # SFC 컴포넌트
│   │   ├── composables/           # 124개 (.ts, test 제외)
│   │   ├── stores/                # Pinia 1개 (repos.ts)
│   │   ├── pages/                 # 4개 (unplugin-vue-router 자동)
│   │   ├── api/                   # IPC wrapper (8개)
│   │   ├── utils/ types/ locales/ i18n/ styles/
│   │   └── ai/                    # Claude integration
│   └── src-tauri/                 # Rust 백엔드 (.rs 88)
│       ├── src/
│       │   ├── ipc/  (29 .rs)     # Tauri commands
│       │   ├── git/  (36 .rs)     # libgit2 wrapper
│       │   ├── forge/             # Gitea/GitHub
│       │   ├── storage/           # sqlx + migrations
│       │   ├── ai/                # Claude runner
│       │   ├── importer/ pty/
│       │   ├── auth.rs auth_oauth.rs profiles.rs
│       │   ├── error.rs panic_hook.rs secret_mask.rs
│       │   └── alias.rs menu.rs launchpad.rs
│       ├── benches/ tests/ capabilities/ icons/
│       └── Cargo.toml rust-toolchain.toml
├── e2e/                           # Playwright spec × 11
├── docs/                          # plan/(36) analyze/ solutions/ ...
├── bench/  scripts/
└── package.json bun.lock lefthook.yml
```

### 핵심 수치표

| 지표 | 실측 | 비고 |
|----|----|----|
| Vue 파일 (`*.vue`) | 98 | components + pages |
| TypeScript 파일 (`*.ts`) | 233 | src/ 전체 (test 포함) |
| `.test.ts` | 90 | vitest unit |
| Rust 파일 (`*.rs`) | 88 | src-tauri/src/ |
| Rust test 매크로 | 259 | `#[test]`+`#[tokio::test]`+`#[sqlx::test]` |
| Playwright e2e (`*.spec.ts`) | 11 | e2e/ |
| Pinia store | 1 | repos.ts |
| 라우팅 페이지 | 4 | unplugin-vue-router 자동 (vue-router/auto-routes) |
| God component (≥200 LOC) | 35 | StatusPanel 648 최대 |
| IPC 모듈 (.rs) | 29 | src-tauri/src/ipc |
| Git wrapper 모듈 | 36 | src-tauri/src/git |
| i18n leaf | 1311 (ko=en, 대칭 OK) | `scripts/i18n-leaf-count.mjs` SoT |

### God Component Top 20 (≥200 LOC, 실측)

| LOC | 파일 |
|----|----|
| 648 | `components/StatusPanel.vue` |
| 613 | `components/CommitGraph.vue` |
| 535 | `pages/launchpad.vue` |
| 473 | `pages/repositories.vue` |
| 466 | `components/PrDetailModal.vue` |
| 414 | `components/GitKrakenToolbar.vue` |
| 413 | `components/FullscreenDiffView.vue` |
| 399 | `components/CommitDetailSidebar.vue` |
| 370 | `components/BranchPanel.vue` |
| 358 | `components/CommitMessageInput.vue` |
| 341 | `pages/index.vue` |
| 333 | `components/RepoSpecificForm.vue` |
| 327 | `components/CloneRepoModal.vue` |
| 309 | `components/PrPanel.vue` |
| 280 | `components/LfsPanel.vue` |
| 276 | `components/GitKrakenImportModal.vue` |
| 267 | `pages/settings.vue` |
| 261 | `components/RepoTabBar.vue` |
| 260 | `components/StashPanel.vue` |
| 260 | `components/InteractiveRebaseModal.vue` |

> MEMORY 상의 "components/ god comp 0 마일스톤 (c67)" 은 components 디렉토리 한정으로 c67 시점 200줄 미만이었던 것일 가능성. 이후 sprint c74~c89 진행 중 신규/회귀로 30+ 발생. **재달성 우선순위 평가 필요**.

### 라우팅

- `unplugin-vue-router` (devDeps 0.10.9) 자동 라우트 → `pages/*.vue` 4개 → `vue-router/auto-routes` import (`apps/desktop/src/main.ts`).
- `typed-router.d.ts` auto-generated 존재.

### Tauri Capability (보안)

`src-tauri/capabilities/default.json` — `fs:default` 제거 (SEC-010 fix). 활성 permission 9개:
`core:default`, `core:window:default`, `core:window:allow-start-dragging`, `deep-link:default`, `deep-link:allow-get-current`, `dialog:default`, `dialog:allow-open`, `notification:default`, `shell:allow-open`.

---

## 핵심 패턴 (실측 관찰)

### 상태관리

- **Pinia**: store 1개 (`stores/repos.ts`) — 활성 repo 메타 / 탭 / localStorage 영속.
- **vue-query**: `useQuery` 57회 / `useMutation` 108회 (총 165 hook 호출) — query/mutation 모두 composable 안에서 wrapping 패턴.
- **composable-first**: 124개 composable, ref 우선 (reactive 미사용). Pattern 8–14 (caller-decision API / idempotent navigation guard / group collapse / qualifier / SOT fallback / 도메인 prefix) toolkit skill 정착 (vue3-composable-extraction).

### IPC / API

- **이중 경로**: `invoke('cmd', ...)` 직접 호출 172회 + `@/api/...` wrapper import 263회. wrapper 가 우세하나 직접 호출 잔존 — 점진적 수렴 진행 중.
- **`invokeWithTimeout`** wrapper: 30s 기본 / 5분 long-running (clone/fetch/push/ai_*). retry: 네트워크 transient 1회 (clone_/fetch_/pull/push/bulk_fetch), 인증/timeout 제외.
- **devMock**: `api/devMock.ts` 로 Tauri-free dev 모드 지원.

### 에러 처리

- **Frontend 전역**: `app.config.errorHandler` 등록 (`registerGlobalErrorHandler.ts`) → useToast().error() + `invoke('report_frontend_error', ..., timeoutMs:0)` fire-and-forget.
- **Rust**: `thiserror` enum (`error.rs`) — 한국어 메시지 우선, `serde::Serialize` 자동.
- **panic_hook**: `panic_hook.rs` 모듈 분리 (c79 sprint), `tracing target=panic`.

### 인증 / 시크릿

- **keyring 3.4** (apple-native / windows-native / linux-native) — PAT + SSH passphrase.
- **profiles.rs**: `forge_accounts` 테이블 — multi-account.
- **secret_mask.rs**: rate-limit (1초 50건), CRLF escape, env-var mask (c46+ sprint).
- **OAuth**: `auth_oauth.rs` 통합.

### 컴포넌트 합성 (SOT 패턴)

- **VIEW_MODES SOT** (`FullscreenDiffView.vue` — c89 patch): 4 inline button → v-for + 상수.
- **StatusTreeDirRow** (Pattern 14 + 12, c89 patch): 4 dir row 중복 → SOT 컴포넌트.
- **CommitRefPill** (c89 이전): CommitGraph ref-pill 3중복 → SOT.

### 테스트

- vitest 90 unit + 11 Playwright e2e + Rust 259 test 매크로 (cargo test 245 PASS — c89 Phase 1.4 자료).
- Rust 추가 통합 테스트: SQLite pool `acquire_timeout` + tx auto-rollback (Phase 1.4), worktree lifecycle (Phase 1.3 한글 path), Forge httpmock 401/403/429 fault injection 7 case (Phase 1.2), IPC fault injection + auth keyring key format (Phase 1.1).

### i18n

- **SoT**: `apps/desktop/src/locales/{ko,en}.json` — 각 **1311 leaf, symmetry OK**.
- **dup-key 가드** (c89 신규): `scripts/i18n-leaf-count.mjs` 에 JSON top-level dup-key raw regex 검출 추가 — c89-B 의 `gitkrakenImport` 13 leaf silent drop 재발 방지.

---

## 의존성

### 프론트엔드 핵심 런타임 (28 deps + 23 devDeps)

| 그룹 | 패키지 |
|----|----|
| Framework | `vue@3.5.34`, `vue-router@4.6.4`, `pinia@2.3.1` |
| Data | `@tanstack/vue-query@5.100`, `@tanstack/vue-virtual@3.13` |
| UI | `reka-ui@2.9.7`, `vue-draggable-plus@0.6.1`, `@iconify/vue@4.3` |
| Editor | `@codemirror/{lang-css,html,javascript,json,markdown,rust,vue,language,merge,state,view,theme-one-dark}` |
| Terminal | `@xterm/xterm@5.5`, addons `fit`+`web-links` |
| Tauri | `@tauri-apps/api@2.11`, plugins `deep-link@2.4`, `dialog@2.7`, `fs@2.5`, `notification@2.3`, `shell@2.3` |
| Fonts | `@fontsource-variable/roboto-flex`, `@fontsource/noto-sans-kr` |
| Build | `vite@7.3.3`, `vitest@4.1.6`, `vue-tsc@3.2.9`, `typescript@6.0.3`, `eslint@9.39`, `tailwindcss@3.4.19`, `unplugin-{auto-import,vue-components,vue-router}`, `prettier@3.8.3`, `happy-dom@20.9`, `vue-i18n@11.4.2` |

### 백엔드 Rust (28 crates)

| 그룹 | 크레이트 |
|----|----|
| Async/Error | `tokio@1.40 (full)`, `async-trait`, `futures@0.3`, `anyhow`, `thiserror` |
| Serde | `serde@1.0 (derive)`, `serde_json` |
| Git | `git2@0.19 (vendored-libgit2, vendored-openssl)` |
| Encoding | `encoding_rs@0.8`, `unicode-normalization@0.1` |
| DB | `sqlx@0.8 (runtime-tokio, sqlite, macros, migrate, chrono)` |
| HTTP | `reqwest@0.12 (json, rustls-tls)` |
| OS | `keyring@3.4 (apple/windows/linux-native)`, `dirs@5.0`, `tempfile@3.13` |
| Watch | `notify@6.1` ⚠ plan/04 reservation (`docs/plan/04` 참조) |
| Tauri | `tauri@2.1`, plugins `deep-link@2.0`, `dialog@2.0`, `fs@2.0` ⚠ reservation, `notification@2.0`, `shell@2.0` |
| Log/Time | `tracing@0.1`, `tracing-subscriber@0.3 (env-filter)`, `chrono@0.4 (serde)` |
| Misc | `parking_lot@0.12`, `once_cell@1.20`, `url@2.5`, `regex` |

> ⚠ **Reservation lock** (CLAUDE.md User Decision A, c53 confirmed): `notify`, `tauri-plugin-fs` 는 Cargo.toml 21~31 / 52~58 라인 주석에 의도 보존 명시. **삭제/축소 제안 자동 금지** — destructive follow-up 시 별도 사용자 결정.

### 내부 의존성 hub (top 5)

**프론트엔드 (가장 많이 import 되는 컴포넌트):**

1. `BaseModal.vue` — 24회
2. `SkeletonBlock.vue` — 19회
3. `ContextMenu.vue` — 17회
4. `EmptyState.vue` — 11회
5. `MiniSection.vue` — 7회

**백엔드 (mod 사용 빈도):**

1. `crate::` — 174회
2. `std::` — 81회
3. `error::` — 71회
4. `serde::` — 67회
5. `git::` — 57회

### Lockfile 무결성

- `bun.lock` 존재 ✓
- `apps/desktop/src-tauri/Cargo.lock` 존재 ✓
- 최근 c80+ patch bump 14 + c89 Backlog 1~4 후 추가 메이저 격차 없음.

### Dead / unused 의존성

- **`notify`** — 5-Check (Glob/Read/settings/test/docs) 통과하지만 **reservation 보존**. dead 단정 금지.
- **`tauri-plugin-fs`** — 동일 reservation 보존.
- 그 외 모든 prod deps 사용량 5+ 확인 — dead 후보 없음.

---

## Recommendations (개선 제안)

### HIGH — 즉시 조치 후보

1. **God component 35개 회귀 정리** — 증거: `find apps/desktop/src -name "*.vue" -exec wc -l {} \;`, top 5 모두 ≥400 LOC (StatusPanel 648 / CommitGraph 613 / launchpad 535 / repositories 473 / PrDetailModal 466) / 액션: c79~c80 의 god 추출 패턴 (BranchPanel/CommitInput/StashPanel/WorktreePanel) 답습하여 Top 5 부터 추출 — Pattern 8–14 적용 / confidence: **certain**

2. **invoke 직접 호출 172회 wrapper 수렴** — 증거: `grep -rE "invoke\(['\"]" apps/desktop/src` 172 hit + `@/api/` wrapper 이미 263회 사용 → 일관성 회귀 / 액션: lint custom rule 또는 codemod 로 잔존 172건 wrapper 경유로 마이그 / confidence: likely

### MEDIUM — 다음 스프린트 후보

1. **vue-query 활용도 65% 추정** — 증거: useQuery 57 + useMutation 108 = 165 hook, 그러나 IPC 직접 호출 172 잔존 / 액션: query/mutation composable 로 일관 wrapping → 캐시 invalidation/optimistic update 일관 / confidence: likely

2. **Pages 4개 god 동시 잔존** (launchpad 535 / repositories 473 / index 341 / settings 267) — 증거: `find apps/desktop/src/pages` / 액션: page-level container 만 남기고 children components/composables 로 분해 / confidence: likely

3. **Rust test 카운트 SoT 정립** — 증거: `#[test]` 만 카운트 시 167 vs `#[tokio::test]+#[sqlx::test]` 포함 시 259 / cargo test PASS 245 (c89 Phase 1.4) — 3 수치가 모두 다름 / 액션: `cargo test -- --list | wc -l` 결과를 SoT 로 doc/MEMORY 에 통일 / confidence: certain

4. **Tauri IPC catalog drift** (Codex-only) — 증거: 코드 `#[tauri::command]` 실측 171 + ipc/ 29 파일 vs `docs/IMPLEMENTATION-STATUS.md:19` 의 "169 등록 / 26 파일" SoT / 액션: `bun scripts/generate-tauri-commands-index.mjs` 재생성 + IMPLEMENTATION-STATUS.md 19행 갱신 / confidence: certain

5. **sqlx tx rollback path 명시화** (Codex-only) — 증거: [profiles.rs:160-170](apps/desktop/src-tauri/src/profiles.rs#L160-L170) + [git/hide.rs:127-149](apps/desktop/src-tauri/src/git/hide.rs#L127-L149) 모두 `pool.begin` → `tx.commit` 만 있고 `.rollback()` 없음 / 액션: panic/cancellation 경로에서 Transaction Drop 자동 rollback 가정이 안전한지 review — 안전하면 코드 주석으로 의도 명시, 불충분하면 explicit guard 추가 / confidence: likely

### LOW — 여유 있을 때

1. **MEMORY drift 정리** — 증거: MEMORY.md 의 "components/ god comp 0 마일스톤 (c67)" 가 현재 components 디렉토리 god 22개 잔존과 불일치 (`StatusPanel/CommitGraph/PrDetailModal/...`) / 액션: MEMORY entry 갱신 or `/doc-sync` 실행 / confidence: certain

2. **CodeMirror lang 패키지 7종 정합성 점검** — 증거: package.json 의 lang-css/html/javascript/json/markdown/rust/vue 모두 dependency / 액션: 각 lang 의 실제 사용 사례 grep (예: `lang-vue` 가 Vue 코드 diff 에 활용되는지) → 실사용 미만이면 devDep 으로 격하 / confidence: uncertain

### User Decision (자동 액션 금지)

- **`notify` + `tauri-plugin-fs` reservation** — 증거: Cargo.toml 21~31 / 52~58 라인 plan/04 참조 주석 / 가능 액션: (a) 그대로 유지 (b) plan/04 §file-system-watch 구현 후 활성화 (c) reservation 해제 + 의존성 제거 / confidence: certain (의도 보존됨) / 권장: **유지** — c53 시점 결정 재확인됨

- **God component 회귀 우선순위** — 증거: c67 의 "components/ god comp 0 마일스톤" 이후 sprint c74~c89 진행 중 35개 재발생 / 가능 액션: (a) 가장 큰 5개부터 점진 추출 (b) 신규 god 발생 차단 (lint rule) 만 하고 잔존 유지 (c) 전수 분해 sprint / confidence: certain / 권장: **사용자 판단** — 회귀 차단 vs 점진 분해 vs 전수 sprint 트레이드오프

- **`@iconify/vue` 제거 vs 유지** (Codex-only — destructive follow-up) — 증거: [apps/desktop/package.json:56](apps/desktop/package.json#L56) `"@iconify/vue": "^4.3.0"` / 코드 0 hit (`grep -rE "@iconify/vue\|Iconify\|<Icon\b" apps/desktop/src` → 0) / 가능 액션: (a) 그대로 유지 (b) devDep 으로 격하 (c) 제거 (`bun remove @iconify/vue`) / confidence: certain (실측 0 hit) / 권장: **사용자 판단** — Agent 2 의 "dead 후보 없음" 단정과 충돌, Codex 가 false negative 정정. 단 reka-ui icon prop / 별도 SVG inline 으로 대체 가능 여부 사전 점검 후 제거 결정.

---

## Next Suggestion Rules (한국어)

- HIGH/MEDIUM 권고 7건 존재 → **`/plan god-component-regression-cleanup`** 으로 구현 계획을 세울까요? (Top 5 god comp + invoke wrapper 수렴 통합 plan)
- User Decision 2건 존재 → **`/research notify-tauri-plugin-fs-reservation`** 으로 plan/04 §file-system-watch 의 활성 조건을 더 파볼까요?
- MEMORY drift 발견 (components god comp 0 ↔ 실측 22) → **`/doc-sync`** 또는 MEMORY entry 갱신을 권장.

---

## Codex 결과 도착 시 추가 cross-validation 예정 영역

1. Rejected Claims 8건의 Codex 측 verification (특히 vue-query useQuery 57 vs Agent 3 185)
2. God comp 35개 결과의 Codex 독립 확인
3. notify/tauri-plugin-fs reservation 보존 결정의 외부 시각
4. c89 Backlog 1~4 자율 진행 (SOT 2건 + Tauri POC 2건 + i18n dup-key) 의 품질 cross-check
