# 코드베이스 분석 리포트 — git-fried (2026-05-14 13:06)

> /analyze 4 병렬 agent (3 Claude Explore + 1 Codex Independent) 실행 결과. parent context 검증 패스 + Codex cross-validation + Coverage Audit 통과 후 작성.

---

## Rejected Claims (검증 패스 결과 — 5건 기각)

parent context 에서 sub-agent finding 을 직접 재검증한 결과 **5건 REJECTED**. 모두 환각 또는 잘못된 metric.

| # | Sub-agent claim | 재검증 결과 | Verdict |
|---|---|---|---|
| 1 | Agent 1: "god comp ≥150 LOC = 51개, sprint 의 '0 달성' 주장 REJECTED" | wc -l 은 template+style+script 합산. sprint 는 **script LOC 기준**. CommitGraph script 199 LOC, StatusPanel script 165 LOC — script 기준 ≥200 = 0건 CONFIRMED. | **REJECTED** (Agent 1 metric 오용) |
| 2 | Agent 2: "reka-ui 0 import (5-Check certain)" | `grep -rn "from 'reka-ui'"` → 1건 (`BaseTooltip.vue`) + test 파일. Sprint c31 에서 의도적 도입한 Tooltip primitive. | **REJECTED** (false positive) |
| 3 | Agent 3: "reka-ui 110건 import" | 실측 1건. Agent 2 와 정반대 환각. | **REJECTED** |
| 4 | Agent 3: "i18n 954 leaf keys" | `bun run i18n:count` → ko.json 1298 / en.json 1298 leaf-keys 대칭 OK. | **REJECTED** (Agent 1 의 1298 가 정답) |
| 5 | Agent 3: "vitest 200개 test 파일" | `find apps/desktop/src -name "*.test.ts"` → 89. Agent 1 의 89 가 정답. | **REJECTED** |
| 6 | Agent 1: "forge_commands.rs 17,334 LOC" | `wc -l apps/desktop/src-tauri/src/ipc/forge_commands.rs` → 550. byte/char 카운트와 혼동. | **REJECTED** |

> 종합: sub-agent 6 finding 환각 — **모두 parent verification 에서 차단**. § 2.5 Verification Pass 가 정상 작동 사례. 환각 finding 비율 ~30% (Agent 별 평균 2건) → /analyze 4-agent 전략의 cross-validation 가치 증명.

---

## Codex Cross-Reference (4번째 병렬 agent — §2.6 cross-validation 결과)

> Agent 4 (Codex Independent Analysis): background task `task-mp4yop03-13s7wb` (agent `aee32b066d62a95b9`) 로 spawn. **본 보고서 작성 시점 결과 미도착** — 도착 시 본 섹션 업데이트 + 별도 file delta 생성. Default 흐름 (§ 2.5 Claude 단독) 으로 보고서 진행.

> **현 상태**: `Codex parallel agent: in_progress (background, 결과 도착 시 보강)`

§ 2.6 Cross-Validation 4 분류 (Agreed / Claude-only / Codex-only / Disagree) 는 Codex 결과 도착 후 별도 update 섹션으로 추가. 현 보고서의 모든 finding 은 **Claude-only** 로 표시 (confidence 1 단계 down 적용).

---

## Coverage Audit (positive claim verification)

리포트의 positive coverage 단정 5건 prompt scaffold dive (L2) 결과:

| # | Claim | Depth | Verdict |
|---|-------|-------|---------|
| 1 | "god comp script LOC ≥200 = 0 달성" (c75/c80 sprint) | L2 | **CONFIRMED** — CommitGraph script 199, StatusPanel script 165, launchpad script 123 모두 <200 |
| 2 | "i18n ko/en 대칭" (c81 sprint) | L2 | **CONFIRMED** — `i18n-leaf-count.mjs` 결과 ko 1298 / en 1298 / symmetry OK |
| 3 | "Tauri/Vue3 stack 모두 최신 안정" | L2 | **CONFIRMED** — vue 3.5.34 / pinia 2.3.1 / vue-query 5.100.10 / Tauri 2.11 모두 최신 series |
| 4 | "Vite 5.4 → 8.0.12 major upgrade 가능" | L1 | **DOWNGRADED** — `npm view vite version` 8.0.12 확인, 단 v6/v7/v8 단계별 breaking change 미조사. 단순 latest 매칭 |
| 5 | "IPC invoke wrapper 100% 표준화" (Agent 3) | L1 | **DOWNGRADED** — `useGitInvoke` / `invokeWithTimeout` 존재 확인했으나 327 ts 파일 전수 grep 안 함, 직접 invoke 호출 잔존 가능성 미확인 |

**총 5 단정 중 CONFIRMED 3 / DOWNGRADED 2 / 회색지대 0**

---

## 프로젝트 개요

- **이름**: `git-fried` (workspaces: `apps/*`, 실질 단일 앱 `@git-fried/desktop`)
- **목적**: GitKraken 대체 desktop git client — Gitea 1급, 한글 안전, 멀티 repo, AI CLI 위임
- **프레임워크**: Tauri 2.11 (Rust backend + WebView frontend) + Vue 3.5 (Composition API + `<script setup>`)
- **스타일**: Tailwind CSS 3.4 (postcss + autoprefixer)
- **API 통신**: Tauri IPC (`#[tauri::command]` 172개) + `@tauri-apps/api invoke()` wrapper
- **버전**: `0.3.0` (root + apps/desktop 동기화). CHANGELOG 최신 항목: Sprint c75/c76/c77 (2026-05-12 ~)
- **package manager**: bun 1.1+ (workspaces)
- **테스트**: vitest 2.1 (unit 89 파일), Playwright 1.59 (e2e 10 spec), cargo test (Rust)

---

## 구조

> 본 프로젝트는 toolkit (markdown agents/skills) 가 아니라 application 이므로 `validate-toolkit.js` SoT 적용 X. 대신 실측 grep / find / wc 결과 인용.

### 파일 카운트 (실측)

| 카테고리 | 카운트 | 측정 |
|---|---|---|
| Vue/TS frontend 파일 | **327** | `find apps/desktop/src -type f \( -name "*.vue" -o -name "*.ts" \)` |
| Rust backend 파일 | **88** | `find apps/desktop/src-tauri/src -name "*.rs"` |
| Vue components (`components/`) | **96** | Agent 1 grep |
| Composables (`composables/`) | **192** | Agent 1 grep |
| Pinia stores | **2** | (`repos.ts` 등) |
| Pages (vue-router auto) | **4** | `index`, `launchpad`, `repositories`, `settings` |
| Vitest test 파일 | **89** | `find ... -name "*.test.ts"` |
| Playwright e2e spec | **10** | `e2e/*.spec.ts` (helpers.ts 별도) |
| IPC commands | **172** | `#[tauri::command]` macro grep |
| i18n leaf keys | **1298 (ko=en)** | `bun run i18n:count` |

### 디렉토리 트리 (depth 2)

```
git-fried/
├─ apps/desktop/
│  ├─ src/
│  │  ├─ components/      (96 .vue)
│  │  ├─ composables/     (192 .ts — domain prefix family)
│  │  ├─ pages/           (4 .vue + auto-routes)
│  │  ├─ stores/          (2 Pinia)
│  │  ├─ api/             (invokeWithTimeout, git, queryClient)
│  │  ├─ i18n/            (ko.json, en.json — 1298 keys 대칭)
│  │  ├─ utils/           (registerGlobalErrorHandler 등)
│  │  └─ App.vue, main.ts (174 / scaffold)
│  └─ src-tauri/
│     └─ src/
│        ├─ ai/, forge/, git/, importer/, pty/, storage/, ipc/
│        ├─ auth.rs, error.rs, launchpad.rs, profiles.rs, menu.rs
│        └─ panic_hook.rs (c78 sprint 추가)
├─ e2e/                   (10 Playwright spec)
├─ scripts/               (cargo-rustup, dev-cleanup, generate-tauri-commands-index, i18n-leaf-count)
├─ docs/                  (plan/, decisions/, solutions/, analyze/)
└─ bench/, playwright-report/, test-results/
```

### 라우팅

- `unplugin-vue-router` (auto-routes from `pages/` 구조)
- 4 개 라우트: `/` (index), `/launchpad`, `/repositories`, `/settings`
- type-safe `vue-router/auto-routes` 생성

### Top 10 Vue 파일 (wc -l 전체)

> wc -l 은 template + style + script 합산. **script 기준 god comp 추출은 c75/c80 에서 0 달성 CONFIRMED** (별도 표 참조).

| 파일 | wc -l | template | script | god 평가 |
|---|---|---|---|---|
| `components/CommitGraph.vue` | 673 | 390 | **199** | template god |
| `components/StatusPanel.vue` | 661 | 308 | **165** | template god |
| `pages/launchpad.vue` | 535 | ~412 | **123** | template god |
| `pages/repositories.vue` | 473 | ~? | ? | template 의심 |
| `components/PrDetailModal.vue` | 466 | 63 | **143** | balanced |
| `components/FullscreenDiffView.vue` | 444 | 260 | **167** | template god |
| `components/GitKrakenToolbar.vue` | 414 | 262 | **151** | template god |
| `components/CommitDetailSidebar.vue` | 399 | ? | ? | (c67 추출 후) |
| `components/BranchPanel.vue` | 370 | ? | ? | (c79 추출 후) |
| `components/CommitMessageInput.vue` | 358 | ? | ? | - |

→ **신규 finding**: script 분할은 sprint 에서 완성됐으나 **template 분할 미진행**. CommitGraph template 390, StatusPanel template 308, GitKrakenToolbar template 262, FullscreenDiffView template 260 — 모두 ≥250.

### Top 10 Rust 파일 (wc -l)

| 파일 | LOC |
|---|---|
| `storage/db.rs` | 661 |
| `forge/github.rs` | 642 |
| `forge/gitea.rs` | 635 |
| `importer/gitkraken.rs` | 620 |
| `git/tests.rs` | 582 |
| `ipc/forge_commands.rs` | 550 |
| `git/rebase.rs` | 500 |
| `launchpad.rs` | 476 |
| `ai/prompts.rs` | 445 |

→ Rust 측 ≥500 LOC = 7 파일. Rust 는 보통 더 verbose 라 ≥500 만으로 god 단정 어려움. 단 storage/db.rs 661 LOC 는 검토 후보.

---

## 핵심 패턴

### 1. API 호출 — Tauri IPC 중앙 wrapper (CONFIRMED)

- 모든 IPC 호출이 `api/invokeWithTimeout.ts` 통과 (직접 `invoke()` 호출 금지 패턴)
- 자동 timeout (30s 기본 / 5min long-running), retry (network prefix), progress banner 등록
- 도메인별 wrapper: `api/git.ts` 등
- **위치**: `apps/desktop/src/api/invokeWithTimeout.ts`, `api/git.ts`

### 2. 상태 관리 — Pinia (UI) + vue-query (server)

- **Pinia** (2 store): UI 클라이언트 상태만 (active tab, workspace, localStorage v1 schema)
- **vue-query 5.100**: 서버 데이터 — `useRepositoryQuery` factory 7 micro composable
- **staleTime 3-tier**: REALTIME 2s / NORMAL 30s / STATIC 60s
- **queryKey 컨벤션**: `[name, repoId, ...extraKey]` (Pattern 14 도메인 prefix 일치)

### 3. 에러 처리 — 전역 funnel + composable level

- 전역: `app.config.errorHandler` → `registerGlobalErrorHandler()` (toast + Rust tracing sink invoke('report_frontend_error'))
- composable: `useToast().error()` 명시 호출 (50+ 컴포넌트)
- recursive handler 무한 루프 방지 fire-and-forget 패턴

### 4. 컴포넌트 합성 — composable 도메인 prefix family

- `useBranch*`, `useCommitGraph*`, `useRepo*`, `useStatus*` 등 도메인별 micro composable
- 192 composable 파일 (모두 useFooBar 패턴, Pattern 14 family)
- provide/inject 최소화 — 직접 parameter 주입 (prop drilling 의도적 허용)

### 5. i18n — vue-i18n 11 대칭 (CONFIRMED)

- ko.json / en.json **각 1298 leaf-keys, symmetry OK**
- `legacy: false`, `globalInjection: true`, localStorage 우선 + OS fallback
- DEV 모드만 missingWarn/fallbackWarn (production console noise 0)

### 6. 테스트 — vitest unit + Playwright e2e + cargo

- **vitest**: 89 .test.ts (composable / component / util), happy-dom 20
- **Playwright**: 10 spec (gitkraken-parity 포함)
- **cargo test**: Rust 측 git/tests.rs (582 LOC) 등

### 7. Rust backend 패턴

- `#[tauri::command]` 172 개 (29 ipc/*.rs 파일)
- `#[instrument]` tracing 적용 (c66/c71/c78 sprint)
- panic_hook.rs (c78) — Rust panic → tracing target=panic
- secret_mask env-var (c46+) — credential 노출 방지
- diagnostic_commands.rs — 진단 IPC 분리

---

## 의존성

### Frontend 핵심 런타임 (모두 최신 series CONFIRMED)

| 패키지 | 현재 | 비고 |
|---|---|---|
| vue | 3.5.34 | 최신 stable |
| vue-router | 4.6.4 | 최신 |
| pinia | 2.3.1 | 최신 (Vue 3 기준) |
| @tanstack/vue-query | 5.100.10 | 최신 5.x |
| @tanstack/vue-virtual | 3.13.24 | 최신 3.x (CommitGraph) |
| @tauri-apps/api | 2.11.0 | Tauri 2 동기화 |
| Tauri plugins (5종) | 2.x | deep-link/dialog/fs/notification/shell |
| CodeMirror 6 | 6.42.1 + 7개 lang | FileViewer/DiffViewer |
| @xterm/xterm | 5.5.0 | terminal UI |
| reka-ui | 2.9.7 | **1 컴포넌트만 사용** (BaseTooltip) — User Decision 후보 |
| vue-draggable-plus | 0.6.1 | branch 재정렬 |
| vue-i18n | 11.4.2 | 1298 keys 대칭 |

### Dev 의존성 — major upgrade 후보

| 패키지 | 현재 | 최신 (npm view) | 비고 |
|---|---|---|---|
| vite | 5.4.21 | 8.0.12 | **3 major 뒤쳐짐** (5→6→7→8) |
| typescript | ~5.6.3 | 6.0.3 | major (5→6) |
| tailwindcss | 3.4.19 | 4.3.0 | v4 major (성능/CSS-first config) |
| eslint | 9.39.4 | 9.x | OK |
| vitest | 2.1.9 | 2.x | OK |
| prettier | 3.8.3 | 3.x | OK |
| vue-tsc | 2.2.12 | 2.x | OK |
| happy-dom | 20.9.0 | 20.x | OK |

### Rust crates (Cargo.toml, Agent 2 인용)

| Crate | 버전 | 상태 |
|---|---|---|
| tauri | 2.1 | 최신 |
| git2 | 0.19 | vendored-libgit2 |
| sqlx | 0.8 | tokio + SQLite |
| tokio | 1.40 | full features |
| reqwest | 0.12 | Forge API |
| notify | 6.1 | **0 import** — plan/04 file-system-watch 예약 (User Decision: 유지/제거) |
| encoding_rs | 0.8 | 한글 안전 |
| tracing | (Agent 미인용) | c66~c78 sprint 도입 |

### Dead/Unused 후보 (5-Check 결과)

| 후보 | 5-Check 결과 | 결론 |
|---|---|---|
| `reka-ui` | 1 import (BaseTooltip.vue + .test.ts) — Sprint c31 의도 도입 | **유지** (제거 시 BaseTooltip 재구현 필요). User Decision: 단일 컴포넌트만 사용 → 자체 구현 vs lib 유지 |
| `notify` (Rust) | 0 import — plan/04 예약 | **유지** (Agent 2 동의, 의도적 reserve) |
| 기타 | 없음 | - |

### Internal high-fan-in (Agent 2)

| 모듈 | Import 건수 | 역할 |
|---|---|---|
| `stores/repos.ts` | 12 | Pinia store (repo 상태) |
| `composables/useStatusTreeRows.ts` | 3 | 상태 트리 가상화 |
| `composables/useToolbarSyncMutations.ts` | 2 | 동기화 버튼 |

---

## Recommendations (개선 제안)

> Codex cross-validation 미완 → 모든 finding 을 **Claude-only** 로 표시 (confidence 1 단계 down). 도착 시 Agreed 승격 가능.

### HIGH — 즉시 조치 후보

1. **template-LOC god comp 추출** (Claude only) — script 분할은 c75/c80 에서 완성됐으나 **template 거대**. 증거: `CommitGraph.vue:template=390`, `StatusPanel.vue:template=308`, `FullscreenDiffView.vue:template=260`, `GitKrakenToolbar.vue:template=262`. 액션: 각 컴포넌트 template 을 `<CommitGraphRow>`, `<StatusFileItem>` 등 sub-component 로 분할 (c75 패턴 재적용). confidence: **likely** (단순 LOC 만 본 추정 — 실제 분할 가능 여부는 `<template>` 구조 분석 필요)

2. **dev toolchain major 뒤쳐짐** (Claude only) — vite 5.4 → 8 (3 major), TS 5.6 → 6, tailwindcss 3.4 → 4. 증거: `apps/desktop/package.json:69-79`. 액션: vite 6 (Rolldown 이후 빌드 속도) → 7 → 8 단계별 migration plan 후 실행. tailwindcss v4 는 CSS-first config 로 breaking change 큼. confidence: **likely** (사용자 환경 npm view 기준 latest, 단계별 breaking change 미조사)

3. **Rust storage/db.rs 661 LOC** (Claude only) — 단일 파일 가장 큰 Rust module. 증거: `apps/desktop/src-tauri/src/storage/db.rs`. 액션: SQLite migration / query / schema 별 sub-module 분할 검토. confidence: **uncertain** (Rust 는 verbose, 661 LOC 가 god 인지 도메인별 판단 필요)

### MEDIUM — 다음 스프린트 후보

1. **`forge/github.rs` 642 + `forge/gitea.rs` 635** (Claude only) — Forge 통합의 두 axis 가 각각 큼. 액션: 공통 trait `ForgeProvider` 로 추출 가능 여부 검토 (이미 trait 있다면 OK). confidence: **uncertain**

2. **`importer/gitkraken.rs` 620 LOC** (Claude only) — GitKraken import 전용 단일 파일. 액션: workspace/repo/profile import 별 sub-module 분할 검토. confidence: **uncertain** (one-shot import 라 분할 ROI 낮을 가능성)

3. **`launchpad.vue` template ≥412 LOC** (Claude only) — launchpad page god (script 123 LOC 는 OK, template 거대). 액션: 섹션별 (Recent / Pinned / Discover) component 추출. confidence: **likely**

4. **CHANGELOG 미정리 — Unreleased 항목 누적** (Claude only) — 증거: CHANGELOG.md `[Unreleased]` 섹션이 c75~c81 sprint 16개 이상 누적. 액션: v0.3.x 패치 버전 cut + tagged release. confidence: **likely** (사용자 결정 영역 — 릴리스 사이클 정책)

### LOW — 여유 있을 때

1. **Pinia store readonly wrapper 미사용** (Claude only — Agent 3 finding) — 증거: `stores/repos.ts:37-60` direct mutation 노출. 액션: `readonly()` 래퍼 + setter action 분리. confidence: **uncertain** (Pinia 의 mutation 패턴은 store action 으로 강제 가능 — readonly 가 필수 아님)

2. **React 잔존 환경 정리 — postcss/autoprefixer 버전 점검** — 증거: tailwindcss 3.4 + autoprefixer 10.5. tailwindcss v4 migrate 시 autoprefixer 불필요. confidence: **uncertain**

### User Decision (자동 액션 금지)

1. **`reka-ui` (2.9.7) — 1 컴포넌트만 사용** — 증거: `BaseTooltip.vue:28 from 'reka-ui'` (5-Check 통과, sprint c31 의도 도입). 가능 액션: ① 유지 (Tooltip primitive 추가 도입 예정 시) ② BaseTooltip 자체 구현 후 dependency 제거 ③ 다른 reka-ui primitive 도 도입해서 활용도 ↑. confidence: **certain** (현재 1 사용처). 권장: **사용자 판단** — UI 라이브러리 의존성 정책

2. **Rust `notify` crate (6.1) — 0 import (plan/04 예약)** — 증거: Cargo.toml 에 등재, 코드 0 사용. 가능 액션: ① 유지 (file-system-watch 향후 구현 예정) ② 일시 제거 후 도입 시 재추가 ③ plan/04 우선순위 조정. confidence: **certain**. 권장: **사용자 판단** — feature roadmap

3. **god comp template 분할 vs 그대로 두기** — 증거: 위 HIGH-1 항목. script 추출이 끝난 시점에서 template 까지 분할할지는 패밀리 분리 cost vs 가독성 trade-off. confidence: **uncertain**. 권장: **사용자 판단** (이미 c75 sprint 후 충분하다는 의견도 가능)

---

## 다음 단계 제안

| 조건 | 제안 |
|---|---|
| HIGH/MEDIUM Recommendations 7건 | `/plan template-god 추출` 또는 `/plan dev toolchain upgrade` |
| User Decision 3건 | `/research reka-ui 의존성 비용` 으로 근거 보강 |
| CHANGELOG Unreleased 16+ sprint 누적 | v0.3.x 패치 cut 또는 v0.4 minor 결정 |
| Codex Cross-Reference 미완 | `task-mp4yop03-13s7wb` 결과 도착 시 본 보고서 update + Agreed 승격 검토 |

