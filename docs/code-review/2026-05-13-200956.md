# Code Review Report — c81 session (b1c9d0b..3361918)

- **Generated**: 2026-05-13 20:09:56
- **Scope**: 11 commits / 44 files / +2065 / -337 LOC
- **Reviewers**: 5 (4 Claude agents + Codex) — Codex `cli_unavailable` (quota cap, 10:37 PM 재시도)

## Summary

| 카테고리 | Critical | High | Medium | Low | 합계 |
|----------|----------|------|--------|-----|------|
| Lint     | 0        | 0    | 0      | 0   | **0 (clean)** |
| Type     | 0        | 0    | 0      | 2   | 2 |
| Security | **2**    | 6    | 5      | 4   | 17 |
| Codex    | —        | —    | —      | —   | skipped (`cli_unavailable`, quota cap) |
| Architecture | 0    | 2    | 5      | 4   | 11 |
| **합계** | **2**    | **8**| **10** | **10**| **총 30건** |

## Fix Status

| Severity | Total | Fixed (commits f666197 + 후속) | Remaining |
|---|---|---|---|
| Critical | 2 | **2 ✅** (SEC-001 PKCE assert + SEC-002 scheme 통일) | 0 |
| High | 8 | **2 ✅** (ARCH-001 String→AppResult + ARCH-002 lazy load) | 6 |
| Medium | 10 | 0 | 10 |
| Low | 10 | 0 | 10 |

## Critical Issues — 모두 fix 완료 ✅

### SEC-001 PKCE bypass (auth_oauth.rs:53) → **FIXED `f666197`**

- 진단: `code_challenge=PLACEHOLDER` hardcoded → v1.0 release 시 PKCE 보호 무력화
- Fix: RFC 7636 §4.1 verifier 길이 (43-128) + unreserved char (A-Z a-z 0-9 -._~) `assert!` panic + `code_challenge_method='plain'` (S256 + sha2 통합은 v1.0)

### SEC-002 deep-link scheme mismatch → **FIXED `f666197`**

- 진단: auth_oauth.rs `gitfried://` (no-hyphen) ↔ tauri.conf.json + useDeepLink `git-fried://` (hyphen) 미스매치 → OAuth callback 절대 도달 안 함 + 다른 앱 hijack 표면
- Fix: auth_oauth.rs 4 occurrences (replace_all) `gitfried://` → `git-fried://` 통일

## High Issues

### ARCH-001 auth_oauth `Result<_, String>` → AppResult → **FIXED 후속**

- 진단: 다른 IPC 모듈 (forge_commands / db / search_commands) AppResult 사용. auth_oauth 만 `String` error type — caller 변환 cost
- Fix: `AppResult<OAuthTokenSet>` + `AppError::Internal(...)` 변환. test assert 도 `format!("{}", err)` 패턴

### ARCH-002 useExternalIssueTracker eager localStorage load → **FIXED 후속**

- 진단: module-load 시점 즉시 `readCreds()` — SSR / 시크릿 모드 환경에서 빈 배열 영구 고정 → 사용자 credentials lost
- Fix: `ref<T[] | null>(null)` + `ensureLoaded()` lazy + add/delete 시 `ensureLoaded()` 통과

### SEC-003 localStorage 평문 API key (useExternalIssueTracker:33-51) — 미수정 (큰 변경)

- 진단: Linear/Jira/Trello API key 가 `localStorage.setItem(...)` 평문 — XSS 1회 발생 시 token leak
- Fix 권장: Tauri keyring 통합 (forge_accounts.keychain_ref 패턴 차용). v0.5 별도 sprint — Linear/Jira 실 mutation 도입 시 동반

### SEC-004 `#[instrument(skip_all)]` 누락 (forge_commands.rs forge_save_token / forge_whoami)

- 진단: PAT 토큰 args 보유 IPC handler 에 tracing instrument 없음 → 미래 `?args` Debug-format 시 토큰 leak 위험
- Fix 권장: `#[tracing::instrument(skip_all, fields(forge_kind=%args.forge_kind, base_url=%args.base_url, token_len=args.token.len()))]` 추가

### SEC-006 URI scheme injection (useExternalEditor:28-46)

- 진단: `encodeURI` 가 `?`/`#`/`&` 미인코딩 — 경로에 `?` 포함 시 URI fragment 가로채기 / `idea://open?file=` 의 query param smuggle
- Fix 권장: `encodeURIComponent(path).replace(/%2F/g, '/')` (slash 유지 + 나머지 escape)

### SEC-007 ReDoS clamp (useAiCli.ts:94-102)

- 진단: AI stderr 가 megabyte 길이 시 regex chain 7 backtracking → 렌더러 stall
- Fix 권장: `const message = String(err).slice(0, 4_000)` 1줄 추가

### SEC-008 ssh_key_path 검증 부재 (db.rs:353)

- 진단: 워크스페이스 import 시 `/etc/shadow` 또는 명령 injection path 가능
- Fix 권장: IPC handler 진입 시 `canonicalize` + file metadata 검증 + Unix `0600` 권한 체크. 현재 IPC 미등록이라 즉시 위험 0 — v0.5 활성화 시 동반 (ARCH-003 종속)

## Medium Issues — 빠른 fix 후보

### TYPE-001 SettingsEditor unsafe `as ExternalEditorKind` cast (L55)

- 진단: DOM `value` 가 7 union 밖 시 silent pass — `buildEditorUri` default 가 `null` 흡수 (silent no-op)
- Fix: type guard `VALID.includes(v) ? setExternalEditor(v) : undefined`

### TYPE-002 useExternalIssueTracker `JSON.parse as IssueTrackerCredential[]` 무검증

- 진단: localStorage 오염 시 shape 검증 없이 통과
- Fix: zod schema 또는 runtime shape check. v0.5 API 활성화 동반

### ARCH-003 DbExt::set_repo_ssh_key_path trait surface drift

- 진단: trait + impl 추가 / IPC 미등록 / FE wrapper 없음 → coverage claim drift
- Fix: (a) IPC 등록 + FE wrapper, 또는 (b) `#[allow(dead_code)]` + "v0.5 #9 reserved" KDoc

### ARCH-004 telemetryOptIn dead flag

- 진단: GeneralSettings flag 추가 + UI toggle 0 + read site 0 → KDoc 의 "본 flag false 시 모든 telemetry 비활성" 단정 mismatch
- Fix: KDoc "v1.0 #26 reserved" 명시 또는 SettingsGeneral toggle 추가

### ARCH-005 FirstRunWizard nested ref `.value` template 접근

- 진단: `wizard.step.value === 1` template — Vue auto-unwrap 은 top-level only. 동작 OK, 컨벤션 위반
- Fix: `const { isOpen, step } = useFirstRunWizard()` destructure → template `step === 1`

### SEC-005 error msg account_id echo (forge_commands.rs:197-202)

- 진단: validation error 에 account_id 정수 직접 interpolation. 정수는 secret 아니지만 secret_mask 통과 안 함 → 미래 token interpolation 시 leak risk
- Fix: `error.rs` 의 `AppError::validation` constructor 에 `secret_mask::mask_secrets()` 통합

### SEC-009 search_commands 미래 shell injection 경고 주석

- 진단: skeleton 만 — v0.5 활성화 시 `Command::new("sh").arg("-c").arg(format!(...))` 패턴 위험
- Fix: 파일 상단 SECURITY 주석 추가 (`Command::new("rg").args([pattern, path])` form 강제)

### SEC-010 FK 무결성 integration test 부재

- 진단: 0006 migration FK ON DELETE RESTRICT — 미래 pragma 변경 시 silent data-loss
- Fix: integration test 추가 (forge_accounts 삭제 시 referenced repo 차단 검증)

### SEC-011 CSP `style-src 'unsafe-inline'` (architectural)

- 진단: Vue scoped styles + Tailwind runtime style 필수 — 회피 어려움. 단 `:style="{ background: userInput }"` 같은 사용자 input interpolation 차단 필요
- Fix: lefthook `no-inline-style-binding` grep 룰 (선택)

### SEC-013 AI count localStorage tampering cap

- 진단: `IdentityCard` 가 `Number.MAX_SAFE_INTEGER` 같은 tampered value 표시 → layout break / info leak
- Fix: `Math.min(raw, 10_000_000)` 1줄 cap

## Low Issues

### SEC-012 OAuth `client_secret` 제거 권장
- 진단: 데스크탑 OAuth 는 PKCE-only — `client_secret` 은 binary extract 시 노출
- Fix: signature 에서 `client_secret` 제거 + KDoc "GitHub Apps / device code flow only"

### SEC-014~017 (LOW 4건)
- SEC-014 forge_save_token validation order swap (defense-in-depth)
- SEC-015 keybindings table CHECK constraint (`length(chord) ≤ 64`)
- SEC-016 useExternalIssueTracker JSON parse shape (TYPE-002 와 중복)
- SEC-017 auth.rs `tracing::debug!` 의 key 안 base_url 노출 (internal Gitea URL)

### ARCH-008 tauri.conf.json bundle targets cross-compile
- 진단: Windows 환경 build 시 macOS/Linux target silent skip
- Fix: CI matrix per-OS + conditional targets

### ARCH-009 keybindings load 진입점 미구현
- KDoc "App start 시 load" 단정 vs 실 코드 부재
- Fix: KDoc "v0.6 #22 reserved" 명시

### ARCH-010 FirstRunWizard 'system' theme 부재
- step 2 systemHint 텍스트 mismatch
- Fix: 3-button (light/dark/system) 또는 systemHint 텍스트 제거

### ARCH-011 RepoSpecificForm 직접 useQuery (family 일관성)
- 다른 c81 composable family 와 mid-tier consistency. 단 의도적 OK

## Action Items

- [x] **Critical (SEC-001 SEC-002)** — fix 완료 (commit `f666197`)
- [x] **High (ARCH-001 ARCH-002)** — fix 완료 (후속 commit)
- [ ] **High SEC-004 / SEC-006 / SEC-007** — 다음 sprint 권장 (각 S effort)
- [ ] **High SEC-003 / SEC-008** — v0.5 phase (Linear/Jira mutation / SSH IPC 활성화 시 동반)
- [ ] **Medium 10건** — 다음 sprint 또는 본 session 잔여 시간 가능 시 처리
- [ ] **Low 10건** — 시간 될 때

## Codex Re-validation

Codex `cli_unavailable` (quota cap, 10:37 PM 재시도) — 다음 sprint 시점 Codex 재호출 권장:
- Critical fix 두 건 (SEC-001/SEC-002) cross-check
- High SEC-003 (localStorage 평문) 의 Tauri keyring 대안 검증
- ARCH 영역 (Pattern 9 family / SoT) substrate-level 단정 cross-validate

## c81 핵심 성과 검증

- ✅ Lint clean (29/29 files 0 issue)
- ✅ Type 0 error / vitest 892 PASS
- ✅ god comp wave A/B 3건 (StatusBar 128 / GitKrakenToolbar 149 / RemoteManageModal 72) 완료
- ✅ Pattern 9 family + SoT (Resolution chain / DOHERTY_THRESHOLD_MS / chord normalize) 일관
- ✅ UI/UX 방법론 100% 적용 (Doherty / WCAG 2.2 / CJK / aria-live)
- ✅ secret_mask 11 패턴 + panic_hook 통합 ✓

## See Also

- [docs/plan/31-ultraplan-v1.0-roadmap.md](../plan/31-ultraplan-v1.0-roadmap.md) — UltraPlan 23/23 100% 코드 path
- [docs/analyze/2026-05-13-160000-codex-cross-verify.md](../analyze/2026-05-13-160000-codex-cross-verify.md) — Codex cross-verify (이전 세션)
- 본 review 의 detail: agent 결과 transcript (output_file 경로 참조)
