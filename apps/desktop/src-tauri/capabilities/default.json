{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability — git-fried desktop default. SEC-010 fix: fs:default 제거 (frontend 가 plugin-fs 를 직접 호출하지 않음 — 모든 file IO 는 Rust IPC 경유. plugin 자체는 v0.5+ 대비 보존.)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-start-dragging",
    "deep-link:default",
    "deep-link:allow-get-current",
    "dialog:default",
    "dialog:allow-open",
    "notification:default",
    "shell:allow-open"
  ]
}
