# dependencies
node_modules/
bun.lock
package-lock.json
yarn.lock
pnpm-lock.yaml

# build outputs
dist/
dist-ssr/
.output/
*.local
target/
*.tsbuildinfo

# tauri (gitignore 패턴은 separator 있으면 root-relative — apps/* 와 매칭되도록 ** 사용)
**/src-tauri/target/
**/src-tauri/gen/
**/src-tauri/WixTools/

# vite auto-generated
auto-imports.d.ts
components.d.ts
typed-router.d.ts

# vite config build cache (vue-tsc -b → .d.ts + esbuild → .js)
**/vite.config.d.ts
**/vite.config.js
**/vite.config.timestamp-*

# editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# misc
.cache/
*.bak

# Playwright MCP dogfood artifacts (local-only verification)
.playwright-mcp/
c25-*.png
c27-*.png
playwright-report/
test-results/

# vitest --coverage report
coverage/

# /plan artifacts (temporary, untracked)
plan.md
plan-*.md
post-commit-*.yml

# Playwright MCP dogfood/audit screenshots (local-only)
audit-*.png
audit-*.yml
git-fried-*.png
snapshot-*.md
.claude/agent-memory/
.claude/settings.local.json
checkpoint.md

# UI/UX 흡수 sprint — capture artifacts (사용자 작업 화면 / 시크릿 위험)
docs/ux-eval/screenshots/*.png
docs/ux-eval/screenshots/session-*.json
!docs/ux-eval/screenshots/README.md

# Plan #40 — GitKraken AHK v2 자동 제어 toolchain
# - AHK v2 portable binary (3MB, autohotkey.com 재배포 — repo 미보관)
# - hello-result.log + ahk-v2.zip 자체도 binary
# - Phase 2+ 자동 캡처 산출물 (handson/screenshots) 도 시크릿 위험으로 미커밋
bench/gitkraken-spike/ahk-v2/
bench/gitkraken-spike/ahk-v2.zip
bench/gitkraken-spike/hello-result.log
docs/ux-eval/handson/screenshots/*.png
!docs/ux-eval/handson/screenshots/README.md

